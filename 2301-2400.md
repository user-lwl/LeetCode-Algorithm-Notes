# 2303.计算应缴税款总额

```java
class Solution {
    public double calculateTax(int[][] brackets, int income) {
        int[][] tempBrackets = new int[brackets.length + 2][brackets.length + 2];
        for (int i = 0; i < brackets.length; i++) {
            tempBrackets[i + 1][0] = brackets[i][0];
            tempBrackets[i + 1][1] = brackets[i][1];
        }
        tempBrackets[tempBrackets.length - 1][0] = 9999;
        double result = 0;
        for (int i = 1; i < tempBrackets.length; i++) {
            int need = Math.min(income, tempBrackets[i][0] - tempBrackets[i - 1][0]);
            double fee = (double) need * tempBrackets[i][1] / 100;
            result += fee;
            income -= need;
            if (income == 0) {
                break;
            }
        }
        return result;
    }
}
```



# 2309.兼具大小写的最好英文字母

```java
class Solution {
    public String greatestLetter(String s) {
        Set<Character> bigSet = new HashSet<>();
        Set<Character> smallSet = new HashSet<>();
        for (int i = 0; i < s.length(); i++) {
            char temp = s.charAt(i);
            if (temp >= 'a' && temp <= 'z') {
                smallSet.add(temp);
            } else {
                bigSet.add(temp);
            }
        }
        char result = (char) 0;
        for (Character key : bigSet) {
            if (smallSet.contains((char) (key + ('a' - 'A')))) {
                result = (char) Math.max((int)key, result);
            }
        }
        return result == 0 ? "" : String.valueOf(result);
    }
}
```



# 2315.统计星号

```java
class Solution {
    public int countAsterisks(String s) {
        char[] c = s.toCharArray();
        int result = 0;
        boolean isTrue = true;
        for (int i = 0; i < c.length; i++){
            if (c[i] == '|') {
                isTrue = !isTrue;
                continue;
            }
            if (isTrue && c[i] == '*') {
                result++;
            }
        }
        return result;
    }
}
```



# 2317.操作后的最大异或和

```java
class Solution {
    public int maximumXOR(int[] nums) {
        int result = 0;
        for (int i = 0; i < nums.length; i++) {
            result |= nums[i];
        }
        return result;
    }
}
```



# 2319.判断矩阵是否是一个X矩阵

```java
class Solution {
    public boolean checkXMatrix(int[][] grid) {
        for (int i = 0; i < grid.length; i++) {
            for (int j = 0; j < grid[0].length; j++) {
                if (i == j || i + j == grid.length - 1) {
                    if (grid[i][j] == 0) {
                        return false;
                    }
                } else {
                    if (grid[i][j] != 0) {
                        return false;
                    }
                }
            }
        }
        return true;
    }
}
```



# 2325.解密消息

```java
class Solution {
    public String decodeMessage(String key, String message) {
        Map<Character, Character> kvMap = new HashMap<>();
        char word = 'a';
        for (int i = 0; i < key.length(); i++) {
            char temp = key.charAt(i);
            if (temp == ' ') {
                continue;
            }
            if (!kvMap.containsKey(temp)) {
                kvMap.put(temp, word++);
            }
        }
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < message.length(); i++) {
            char result = kvMap.getOrDefault(message.charAt(i), ' ');
            sb.append(result);
        }
        return sb.toString();
    }
}
```



# 2331.计算布尔二叉树的值

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public boolean evaluateTree(TreeNode root) {
        return getResult(root);
    }

    public boolean getResult(TreeNode node) {
        if (node.val == 0) {
            return false;
        } else if (node.val == 1) {
            return true;
        } else if (node.val == 2) {
            return getResult(node.left) || getResult(node.right);
        } else {
            return getResult(node.left) && getResult(node.right);
        }
    }
}
```

# 2335.装满被子需要的最短总时长

```java
class Solution {
    public int fillCups(int[] amount) {
        Arrays.sort(amount);
        int min = amount[0];
        int middle = amount[1];
        int max = amount[2];
        return max > (middle + min) ? max : (max + middle + min + 1) / 2;
    }
}
```

# 2336.无限集中的最小数字

```java
class SmallestInfiniteSet {

    PriorityQueue<Integer> heap = new PriorityQueue<>();

    public SmallestInfiniteSet() {
        for (int i = 1; i <= 1000; i++) {
            heap.add(i);
        }
    }
    
    public int popSmallest() {
        return heap.poll();
    }
    
    public void addBack(int num) {
        if (!heap.contains(num)) {
            heap.add(num);
        }
    }
}

/**
 * Your SmallestInfiniteSet object will be instantiated and called as such:
 * SmallestInfiniteSet obj = new SmallestInfiniteSet();
 * int param_1 = obj.popSmallest();
 * obj.addBack(num);
 */
```



# 2341.数组能形成多少数对

```java
class Solution {
    public int[] numberOfPairs(int[] nums) {
        int number = 0;
        Map<Integer, Integer> tempMap = new HashMap<>();
        for (int i = 0; i < nums.length; i++) {
            tempMap.put(nums[i], tempMap.getOrDefault(nums[i], 0) + 1);
        }
        int duo = 0;
        for (Integer key : tempMap.keySet()) {
            number += tempMap.get(key) / 2;
            duo += tempMap.get(key) % 2;
        }
        return new int[]{number, duo};
    }
}
```

# 2342.数位和相等数对的最大和

```java
class Solution {
    public int maximumSum(int[] nums) {
        int max = -1;
        Map<Integer, Integer> tempMap = new HashMap<>();
        for (int i = 0; i < nums.length; i++) {
            int temp = nums[i];
            int sum = 0;
            while (temp > 0) {
                sum += temp % 10;
                temp /= 10;
            }
            int number = tempMap.getOrDefault(sum, -1);
            if (number != -1) {
                max = Math.max(max, nums[i] + number);
            }
            tempMap.put(sum, Math.max(number, nums[i]));
        }
        return max;
    }
}
```



# 2347.最好的扑克手牌

```java
class Solution {
    public String bestHand(int[] ranks, char[] suits) {
        int[] suitsList = new int[4];
        for (int i = 0; i < suits.length; i++) {
            suitsList[suits[i] - 'a']++;
        }
        for (int i = 0; i < suitsList.length; i++) {
            if (suitsList[i] == 5) {
                return "Flush";
            }
        }
        int[] ranksList = new int[14];
        for (int i = 0; i < ranks.length; i++) {
            ranksList[ranks[i]]++;
        }
        Arrays.sort(ranksList);
        if (ranksList[ranksList.length - 1] >= 3) {
            return "Three of a Kind";
        } else if (ranksList[ranksList.length - 1] == 2) {
            return "Pair";
        } else {
            return "High Card";
        }
    }
}
```



# 2351.第一个出现两次的字母

```java
class Solution {
    public char repeatedCharacter(String s) {
        Map<Character, Integer> tempMap = new HashMap<>();
        for (int i = 0; i < s.length(); i++) {
            int cnt = tempMap.getOrDefault(s.charAt(i), 0) + 1;
            if (cnt == 2) {
                return s.charAt(i);
            }
            tempMap.put(s.charAt(i), cnt);
        }
        return ' ';
    }
}
```

# 2356.每位教师所教授的科目种类的数量

```sql
# Write your MySQL query statement below
select teacher_id, count(distinct subject_id) cnt from Teacher group by teacher_id;
```



# 2357.使数组中所有元素都等于零

```java
class Solution {
    public int minimumOperations(int[] nums) {
        Set<Integer> tempSet = new HashSet<>();
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] > 0) {
                tempSet.add(nums[i]);
            }
        }
        return tempSet.size();
    }
}
```

# 2363.合并相似的物品

```java
class Solution {
    public List<List<Integer>> mergeSimilarItems(int[][] items1, int[][] items2) {
        Map<Integer, Integer> resultMap = new HashMap<>();
        for (int i = 0; i < items1.length; i++) {
            Integer value = items1[i][0];
            Integer weight = items1[i][1];
            resultMap.put(value, weight);
        }
        for (int i = 0; i < items2.length; i++) {
            Integer value = items2[i][0];
            Integer weight = items2[i][1];
            resultMap.put(value, resultMap.getOrDefault(value, 0) + weight);
        }
        List<List<Integer>> result = new ArrayList<>();
        int[][] tempResult = new int[resultMap.size()][2];
        int index = 0;
        for (Integer key : resultMap.keySet()) {
            Integer value = key;
            Integer weight = resultMap.get(key);
            tempResult[index][0] = value;
            tempResult[index][1] = weight;
            index++;
        }
        Arrays.sort(tempResult, new Comparator<int[]>() {
            public int compare(int[] o1, int[] o2) {
                return o1[0] - o2[0];
            }
        });
        for (int i = 0; i < tempResult.length; i++) {
            List<Integer> tempList = new ArrayList<>();
            Integer value = tempResult[i][0];
            Integer weight = tempResult[i][1];
            tempList.add(value);
            tempList.add(weight);
            result.add(tempList);
        }
        return result;
    }
}
```

# 2364.统计坏数对的数目

```java
class Solution {
    public long countBadPairs(int[] nums) {
        Map<Integer, Integer> tempMap = new HashMap<>();
        int n = nums.length;
        for (int i = 0; i < nums.length; i++) {
            int key = i - nums[i];
            tempMap.put(key, tempMap.getOrDefault(key, 0) + 1);
        }
        long result = 0;
        for (Integer key : tempMap.keySet()) {
            int cnt = tempMap.get(key);
            result += (long) cnt * (n - cnt);
        }
        return result / 2;
    }
}
```

# 2365.任务调度器Ⅱ

```java
class Solution {
    public long taskSchedulerII(int[] tasks, int space) {
        long day = 0;
        Map<Integer, Long> tempMap = new HashMap<>();
        for (int i = 0; i < tasks.length; i++) {
            Long tempDay = tempMap.getOrDefault(tasks[i], 0L);
            if (tempDay == 0) {
                day++;
            } else {
                day = Math.max(day + 1, tempDay + space + 1);
            }
            tempMap.put(tasks[i], day);
        }
        return day;
    }
}
```



# 2367.算数三元组的数目

```java
class Solution {
    public int arithmeticTriplets(int[] nums, int diff) {
        int len = nums.length;
        int number = 0;
        for (int i = 0; i < len; i++) {
            for (int j = i + 1; j < len; j++) {
                if (nums[j] - nums[i] != diff) {
                    continue;
                }
                for (int k = j + 1; k < len; k++) {
                    if (nums[k] - nums[j] == diff) {
                        number++;
                    }
                }
            }
        }
        return number;
    }
}
```

# 2369.检查数组是否存在有效划分

```java
class Solution {
    public boolean validPartition(int[] nums) {
        int n = nums.length;
        boolean[] result = new boolean[n + 1];
        result[0] = true;
        for (int i = 1; i < n; i++) {
            if ((result[i - 1] && nums[i] == nums[i - 1])||(i > 1 && result[i - 2] && ((nums[i] == nums[i - 1] && nums[i] == nums[i - 2])||(nums[i] == nums[i - 1] + 1 && nums[i] == nums[i - 2] + 2)))) {
                result[i + 1] = true;
            }
        }
        return result[n];
    }
}
```

# 2373.矩阵中的局部最大值

```java
class Solution {
    public int[][] largestLocal(int[][] grid) {
        int[][] maxLocal = new int[grid.length - 2][grid[0].length - 2];
        for (int i = 0; i < grid.length - 2; i++) {
            for (int j = 0; j < grid[0].length - 2; j++) {
                int max = 0;
                for (int m = i; m < i + 3; m++) {
                    for (int n = j; n < j + 3; n++) {
                        max = Math.max(max, grid[m][n]);
                    }
                }
                maxLocal[i][j] = max;
            }
        }
        return maxLocal;
    }
}
```

# 2379.得到K个黑块的最少涂色次数

```java
class Solution {
    public int minimumRecolors(String blocks, int k) {
        char[] blocksCharList = blocks.toCharArray();
        int left = 0;
        int right = k - 1;
        int cnt = 0;
        for (int i = left; i <= right; i++) {
            if (blocksCharList[i] == 'W') {
                cnt++;
            }
        }
        int result = cnt;
        left++;
        right++;
        while (right < blocksCharList.length) {
            if (blocksCharList[left - 1] == 'W') {
                cnt--;
            }
            if (blocksCharList[right] == 'W') {
                cnt++;
            }
            result = Math.min(result, cnt);
            left++;
            right++;
        }
        return result;
    }
}
```



# 2383.赢得比赛需要的最少训练时长

```java
class Solution {
    public int minNumberOfHours(int initialEnergy, int initialExperience, int[] energy, int[] experience) {
        int result = 0;
        for (int i = 0; i < energy.length; i++) {
            if (initialEnergy <= energy[i]) {
                result += (energy[i] - initialEnergy + 1);
                initialEnergy = energy[i] + 1;
            }
            initialEnergy -= energy[i];
            if (initialExperience <= experience[i]) {
                result += (experience[i] - initialExperience + 1);
                initialExperience = experience[i] + 1;
            }
            initialExperience += experience[i];
        }
        return result;
    }
}
```

# 2384.最大回文数字

```java
class Solution {
    public String largestPalindromic(String num) {
        int[] tempInt = new int[10];
        for (int i = 0; i < num.length(); i++) {
            int number = num.charAt(i) - '0';
            tempInt[number]++;
        }
        StringBuilder sb = new StringBuilder();
        int moreOne = (tempInt[0] % 2 == 1 ? 0 : -1);
        tempInt[0] = tempInt[0] - moreOne;
        for (int i = tempInt.length - 1; i > 0; i--) {
            if (tempInt[i] % 2 == 1) {
                moreOne = Math.max(moreOne, i);
            }
            int cnt = tempInt[i] / 2;
            while (cnt > 0) {
                sb.append(i);
                cnt--;
            }
        }
        if (sb.length() > 0) {
            int cnt = tempInt[0] / 2;
            while (cnt > 0) {
                sb.append(0);
                cnt--;
            }
        }
        String middle = moreOne == -1 ? "" : String.valueOf(moreOne);
        String left = sb.toString();
        sb.reverse();
        String right = sb.toString();
        if (middle.length() == 0 && left.length() == 0 && right.length() == 0) {
            return "0";
        }
        return left + middle + right;
    }
}
```



# 2389.和有限的最长子序列

```java
class Solution {
    public int[] answerQueries(int[] nums, int[] queries) {
        Arrays.sort(nums);
        int[] sum = new int[nums.length];
        sum[0] = nums[0];
        for (int i = 1; i < sum.length; i++) {
            sum[i] = sum[i - 1] + nums[i];
        }
        int[] result = new int[queries.length];
        for (int i = 0; i < queries.length; i++) {
            for (int j = 0; j < sum.length; j++) {
                if (sum[j] > queries[i]) {
                    result[i] = j;
                    break;
                } else if (j == sum.length - 1 && sum[j] <= queries[i]) {
                    result[i] = j + 1;
                    break;
                }
            }
        }
        return result;
    }
}
```

# 2390.从字符串中移除星号

```java
class Solution {
    public String removeStars(String s) {
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < s.length(); i++) {
            char temp = s.charAt(i);
            if (temp == '*') {
                sb.deleteCharAt(sb.length() - 1);
            } else {
                sb.append(temp);
            }
        }
        return sb.toString();
    }
}
```

# 2391.收集垃圾的最少总时间

```java
class Solution {
    public int garbageCollection(String[] garbage, int[] travel) {
        int needLenM = 0;
        int needLenP = 0;
        int needLenG = 0;
        int[] len = new int[garbage.length];
        System.arraycopy(travel, 0, len, 1, travel.length);
        int result = 0;
        for (int i = 0; i < garbage.length; i++) {
            needLenG += len[i];
            needLenP += len[i];
            needLenM += len[i];
            if (garbage[i].indexOf("G") != -1) {
                result += needLenG;
                needLenG = 0;
            }
            if (garbage[i].indexOf("P") != -1) {
                result += needLenP;
                needLenP = 0;
            }
            if (garbage[i].indexOf("M") != -1) {
                result += needLenM;
                needLenM = 0;
            }
            result += garbage[i].length();
        }
        return result;
    }
}
```



# 2395.和相等的子数组

```java
class Solution {
    public boolean findSubarrays(int[] nums) {
        Set<Integer> sumSet = new HashSet<>();
        for (int i = 0; i < nums.length - 1; i++) {
            int sum = nums[i] + nums[i + 1];
            if (!sumSet.add(sum)) {
                return true;
            }
        }
        return false;
    }
}
```

# 2396.严格回文的数字

```java
class Solution {
    public boolean isStrictlyPalindromic(int n) {
        return false;
    }
}
```



# 2399.检查相同字母间的距离

```java
class Solution {
    public boolean checkDistances(String s, int[] distance) {
        int[] indexList = new int[26];
        for (int i = 0; i < indexList.length; i++) {
            indexList[i] = -1;
        }
        for (int i = 0; i < s.length(); i++) {
            int index = s.charAt(i) - 'a';
            if (indexList[index] == -1) {
                indexList[index] = i;
            } else {
                int len = i - indexList[index] - 1;
                if (distance[index] != len) {
                    return false;
                }
            }
        }
        return true;
    }
}
```

