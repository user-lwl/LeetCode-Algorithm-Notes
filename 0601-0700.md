# 605.种花问题

```java
class Solution {
    public boolean canPlaceFlowers(int[] flowerbed, int n) {
        if (flowerbed[0] == 0 && (flowerbed.length == 1 || flowerbed[1] == 0)) {
            n--;
            flowerbed[0] = 1;
        }
        if (flowerbed[flowerbed.length - 1] == 0 && flowerbed[flowerbed.length - 2] == 0) {
            n--;
            flowerbed[flowerbed.length - 1] = 1;
        }
        for (int i = 1; i < flowerbed.length - 1; i++) {
            if (flowerbed[i] == 0 && flowerbed[i - 1] == 0 && flowerbed[i + 1] == 0) {
                n--;
                flowerbed[i] = 1;
            }
            if (n <= 0) {
                break;
            }
        }
        return n <= 0;
    }
}
```

# 606.根据二叉树创建字符串

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    StringBuilder sb = new StringBuilder();

    public String tree2str(TreeNode root) {
        doHelper(root);
        return sb.toString();
    }

    public void doHelper(TreeNode node) {
        sb.append(node.val);
        if (node.left == null && node.right == null) {
            return;
        } else if (node.left == null) {
            sb.append("()").append("(");
            doHelper(node.right);
            sb.append(")");
        } else if (node.right == null) {
            sb.append("(");
            doHelper(node.left);
            sb.append(")");
        } else {
            sb.append("(");
            doHelper(node.left);
            sb.append(")");
            sb.append("(");
            doHelper(node.right);
            sb.append(")");
        }
    }
}
```



# 607.销售员

```sql
# Write your MySQL query statement below
select name from SalesPerson where sales_id not in (
select sales_id from Orders where com_id = (select com_id from Company where name = 'RED')
);
```

# 609.在系统中查找重复文件

```java
class Solution {
    public List<List<String>> findDuplicate(String[] paths) {
        Map<String, List<String>> tempMap = new HashMap<>();
        List<List<String>> resultList = new ArrayList<>();
        for (int i = 0; i < paths.length; i++) {
            String pathStr = paths[i];
            String[] pathStrList = pathStr.split(" ");
            String basePath = pathStrList[0];
            for (int j = 1; j < pathStrList.length; j++) {
                String tempStr = pathStrList[j];
                String[] tempStrList = tempStr.split("\\(");
                String path = basePath + "/" + tempStrList[0];
                String word = tempStrList[1];
                List<String> tempList = tempMap.getOrDefault(word, null);
                if (tempList == null) {
                    tempList = new ArrayList<>();
                }
                tempList.add(path);
                tempMap.put(word, tempList);
                if (tempList.size() == 2) {
                    resultList.add(tempList);
                }
            }
        }
        return resultList;
    }
}
```



# 610.判断三角形

```sql
# Write your MySQL query statement below
select x, y, z, case when x + y > z and x + z > y and y + z > x then 'Yes' else 'No' end triangle from Triangle;
```



# 611.有效三角形的个数

```java
class Solution {
    public int triangleNumber(int[] nums) {
        Arrays.sort(nums);
        int len = nums.length;
        int result = 0;
        int j;
        int k;
        for (int i = 0; i < len; i++) {
            for (j = i + 1; j < len; j++) {
                for (k = j + 1; k < len; k++) {
                    if (nums[i] + nums[j] <= nums[k]) {
                        break;
                    }
                }
                result += k - j - 1;
            }
        }
        return result;
    }
}
```

# 617.合并二叉树

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public TreeNode mergeTrees(TreeNode root1, TreeNode root2) {
        if (root1 == null) {
            return root2;
        }
        if (root2 == null) {
            return root1;
        }
        TreeNode root = root1;
        doMerge(root1, root2);
        return root;
    }

    public void doMerge(TreeNode node1, TreeNode node2) {
        node1.val = node1.val + node2.val;
        if (node1.left != null && node2.left != null) {
            doMerge(node1.left, node2.left);
        } else if (node1.left == null) {
            node1.left = node2.left;
        } else {
            node2.left = node1.left;
        }
        
        if (node1.right != null && node2.right != null) {
            doMerge(node1.right, node2.right);
        } else if (node1.right == null) {
            node1.right = node2.right;
        } else {
            node2.right = node1.right;
        }
    }
}
```



# 619.只出现一次的最大数字

```sql
# Write your MySQL query statement below
select max(num) num from (
select num, count(*) cnt from MyNumbers group by num
) a where cnt < 2;
```

# 620.有趣的电影

```sql
# Write your MySQL query statement below
select * from cinema where id % 2 = 1 and description <> 'boring' order by rating desc;
```

# 623.在二叉树中增加一行

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    int dep;
    int value;

    public TreeNode addOneRow(TreeNode root, int val, int depth) {
        dep = depth;
        value = val;
        if (dep == 1) {
            TreeNode node = new TreeNode(val);
            node.left = root;
            return node;
        }
        doHelper(root, 1);
        return root;
    }
    public void doHelper(TreeNode node, int depth) {
        if (node == null) {
            return;
        }
        if (depth + 1 == dep) {
            TreeNode left = node.left;
            TreeNode right = node.right;
            TreeNode newLeft = new TreeNode(value, left, null);
            TreeNode newRight = new TreeNode(value, null, right);
            node.left = newLeft;
            node.right = newRight;
        }
        doHelper(node.left, depth + 1);
        doHelper(node.right, depth + 1);
    }
}
```



# 624.数组列表中的最大距离

```java
class Solution {
    public int maxDistance(List<List<Integer>> arrays) {
        int laterMin = arrays.get(0).get(0);
        int laterMax = arrays.get(0).get(arrays.get(0).size() - 1);
        int result = 0;
        for (int i = 1; i < arrays.size(); i++) {
            List<Integer> thisArray = arrays.get(i);
            int thisMin = thisArray.get(0);
            int thisMax = thisArray.get(thisArray.size() - 1);
            int abs1 = Math.abs(thisMax - laterMin);
            int abs2 = Math.abs(thisMin - laterMax);
            result = Math.max(Math.max(abs1, abs2), result);
            laterMin = Math.min(laterMin, thisMin);
            laterMax = Math.max(laterMax, thisMax);
        }
        return result;
    }
}
```



# 626.换座位

```sql
# Write your MySQL query statement below
select * from (
    select
    case when id % 2 = 1 and id = (select count(*) from Seat) then id
         when id % 2 = 1 then id + 1
         else id - 1 end id,
    student
    from Seat
) a order by a.id;
```

# 627.变更性别

```sql
# Write your MySQL query statement below
update Salary set sex = case when sex = 'f' then 'm' else 'f' end;
```

# 628.三个数的最大乘积

```java
class Solution {
    public int maximumProduct(int[] nums) {
        Arrays.sort(nums);
        int len = nums.length;
        return Math.max(nums[len - 1] * nums[len - 2] * nums[len - 3], nums[0] * nums[1] * nums[len - 1]);
    }
}
```



# 637.二叉树的层平均值

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    List<Long> sumList = new ArrayList<>();
    List<Integer> cntList = new ArrayList<>();
    public List<Double> averageOfLevels(TreeNode root) {
        doLevel(root, 1);
        List<Double> resultList = new ArrayList<>();
        for (int i = 0; i < sumList.size(); i++) {
            Double avg = (double) sumList.get(i) / cntList.get(i);
            resultList.add(avg);
        }
        return resultList;
    }

    public void doLevel(TreeNode node, Integer level) {
        if (node == null) {
            return;
        }
        if (level > sumList.size()) {
            sumList.add(0L);
            cntList.add(0);
        }
        Long sum = sumList.get(level - 1);
        int cnt = cntList.get(level - 1);
        sum += node.val;
        cnt++;
        sumList.set(level - 1, sum);
        cntList.set(level - 1, cnt);
        doLevel(node.left, level + 1);
        doLevel(node.right, level + 1);
    }
}
```



# 643.子数组最大平均数Ⅰ

```java
class Solution {
    public double findMaxAverage(int[] nums, int k) {
        int sum = 0;
        int len = nums.length;
        for (int i = 0; i < k; i++) {
            sum += nums[i];
        }
        int maxSum = sum;
        for (int i = 1; i < len - k + 1; i++) {
            sum = sum - nums[i - 1] + nums[i + k - 1];
            maxSum = Math.max(maxSum, sum);
        }
        return (double) maxSum / k;
    }
}
```



# 645.错误的集合

```java
class Solution {
    public int[] findErrorNums(int[] nums) {
        Map<Integer, Integer> resultMap = new HashMap<>();
        int len = nums.length;
        int sum = (1 + len) * len / 2;
        int[] result = new int[2];
        for (int i = 0; i < len; i++) {
            int temp = resultMap.getOrDefault(nums[i], 0);
            if (temp > 0) {
                result[0] = nums[i];
                continue;
            }
            resultMap.put(nums[i], temp + 1);
            sum -= nums[i];
        }
        result[1] = sum;
        return result;
    }
}
```



# 653.两数之和Ⅳ - 输入二叉搜索树

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    List<Integer> list;
    public boolean findTarget(TreeNode root, int k) {
        list = new ArrayList<>();
        inorder(root);
        for (int i = 0; i < list.size(); i++) {
            for (int j = i + 1; j < list.size(); j++) {
                if (list.get(i) + list.get(j) == k) {
                    return true;
                }
            }
        }
        return false;
    }
    public void inorder(TreeNode node) {
        if(node == null) {
            return;
        }
        inorder(node.left);
        list.add(node.val);
        inorder(node.right);
    }
}
```

# 654.最大二叉树

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    int[] globalNums;
    public TreeNode constructMaximumBinaryTree(int[] nums) {
        globalNums = nums;
        TreeNode newHead = new TreeNode(-1);
        doHelper(0, nums.length - 1, newHead, 0);
        return newHead.left;
    }
    // fx  0-left  1-right
    public void doHelper(int left, int right, TreeNode father, int fx) {
        if (left > right) {
            return;
        }
        int max = -1;
        int maxIndex = -1;
        for (int i = left; i <= right; i++) {
            if (globalNums[i] > max) {
                max = globalNums[i];
                maxIndex = i;
            }
        }
        TreeNode node = new TreeNode(max);
        if (fx == 0) {
            father.left = node;
        } else {
            father.right = node;
        }
        doHelper(left, maxIndex - 1, node, 0);
        doHelper(maxIndex + 1, right, node, 1);
    }
}
```



# 657.机器人能否返回原点

```java
class Solution {
    public boolean judgeCircle(String moves) {
        int lrIndex = 0;
        int udIndex = 0;
        for (int i = 0; i < moves.length(); i++) {
            char temp = moves.charAt(i);
            if (temp == 'R') {
                lrIndex++;
            } else if (temp == 'L') {
                lrIndex--;
            } else if (temp == 'U') {
                udIndex++;
            } else {
                udIndex--;
            }
        }
        return lrIndex == 0 && udIndex == 0;
    }
}
```



# 661.图片平滑器

```java
class Solution {
    public int[][] imageSmoother(int[][] img) {
        int m = img.length;
        int n = img[0].length;
        int[][] result = new int[m][n];
        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                result[i][j] = avg(img, i, j);
            }
        }
        return result;
    }

    public int avg(int[][] img, int i, int j) {
        int sum = 0;
        int num = 0;
        for (int x = i - 1; x <= i + 1; x++) {
            for (int y = j - 1; y <= j + 1; y++) {
                if (x >= 0 && y >= 0 && x < img.length && y < img[0].length) {
                    sum += img[x][y];
                    num++;
                }
            }
        }
        return sum / num;
    }
}
```

# 670.最大交换

```java
class Solution {
    public int maximumSwap(int num) {
        String tempStr = String.valueOf(num);
        char[] tempCharArray = tempStr.toCharArray();
        char[] tempCharArraySortChar = tempStr.toCharArray();
        Character[] tempCharArraySort = new Character[tempCharArraySortChar.length];
        for (int i = 0; i < tempCharArraySort.length; i++) {
            tempCharArraySort[i] = tempCharArraySortChar[i];
        }
        Arrays.sort(tempCharArraySort, new Comparator<Character>() {
            @Override
            public int compare(Character a, Character b) {
                return b - a;
            }
        });
        Character needChar = null;
        int needIndex = -1;
        for (int i = 0; i < tempCharArray.length; i++) {
            if (tempCharArray[i] != tempCharArraySort[i]) {
                needIndex = i;
                needChar = tempCharArraySort[i];
                break;
            }
        }
        if (needIndex != -1) {
            int otherIndex = -1;
            for (int i = tempCharArray.length - 1; i >= 0; i--) {
                if (tempCharArray[i] == needChar) {
                    otherIndex = i;
                    break;
                }
            }
            char temp = tempCharArray[needIndex];
            tempCharArray[needIndex] = tempCharArray[otherIndex];
            tempCharArray[otherIndex] = temp;
        }
        return Integer.valueOf(new String(tempCharArray));
    }
}
```



# 671.二叉树中第二小的节点

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    Set<Integer> tempSet = new HashSet<>();

    public int findSecondMinimumValue(TreeNode root) {
        getVal(root);
        if (tempSet.size() < 2) {
            return -1;
        }
        int[] tempInt = new int[tempSet.size()];
        int index = 0;
        for (Integer key : tempSet) {
            tempInt[index++] = key;
        }
        Arrays.sort(tempInt);
        return tempInt[1];
    }

    public void getVal(TreeNode node) {
        if (node == null) {
            return;
        }
        tempSet.add(node.val);
        getVal(node.left);
        getVal(node.right);
    }
}
```



# 674.最长连续递增序列

```java
class Solution {
    public int findLengthOfLCIS(int[] nums) {
        int left = 0;
        int right = 0;
        int maxLen = 1;
        for (int i = 1; i < nums.length; i++) {
            if (nums[i] <= nums[i - 1]) {
                right = i - 1;
                maxLen = Math.max(maxLen, right - left + 1);
                left = i;
                right = i;
            }else if (i == nums.length - 1) {
                right = i;
                maxLen = Math.max(maxLen, right - left + 1);
            }
        }
        return maxLen;
    }
}
```

# 678.有效的括号字符串

```java
class Solution {
    public boolean checkValidString(String s) {
        Stack<Integer> leftStack = new Stack<>();
        Stack<Integer> startStack = new Stack<>();
        for (int i = 0; i < s.length(); i++) {
            char temp = s.charAt(i);
            if (temp == '(') {
                leftStack.push(i);
            } else if (temp == '*') {
                startStack.push(i);
            } else {
                if (!leftStack.isEmpty()) {
                    leftStack.pop();
                } else if (!startStack.isEmpty()) {
                    startStack.pop();
                } else {
                    return false;
                }
            }
        }
        while (!leftStack.isEmpty() && !startStack.isEmpty()) {
            if (leftStack.peek() < startStack.peek()) {
                leftStack.pop();
                startStack.pop();
            } else {
                return false;
            }
        }
        return leftStack.isEmpty();
    }
}
```



# 680.验证回文串Ⅱ

```java
class Solution {
    public boolean validPalindrome(String s) {
        char[] sChar = s.toCharArray();
        int left = 0;
        int right = sChar.length - 1;
        while (left < right) {
            if (sChar[left] != sChar[right]) {
                break;
            }
            left++;
            right--;
        }
        if (left >= right) {
            return true;
        }
        int temp1Left = left + 1;
        int temp1Right = right;
        int temp2Left = left;
        int temp2Right = right - 1;
        while (temp1Left < temp1Right) {
            if (sChar[temp1Left] != sChar[temp1Right]) {
                break;
            }
            temp1Left++;
            temp1Right--;
        }
        if (temp1Left >= temp1Right) {
            return true;
        }
        while (temp2Left < temp2Right) {
            if (sChar[temp2Left] != sChar[temp2Right]) {
                break;
            }
            temp2Left++;
            temp2Right--;
        }
        if (temp2Left >= temp2Right) {
            return true;
        }
        return false;
    }
}
```



# 682.棒球比赛

```java
class Solution {
    public int calPoints(String[] operations) {
        List<Integer> tempResult = new ArrayList<>();
        int sum = 0;
        for (int i = 0; i < operations.length; i++) {
            String tempString = operations[i];
            if ("C".equals(tempString)) {
                int tempInt = tempResult.get(tempResult.size() - 1);
                tempResult.remove(tempResult.size() - 1);
                sum -= tempInt;
            } else if ("D".equals(tempString)) {
                int tempInt = tempResult.get(tempResult.size() - 1) * 2;
                tempResult.add(tempInt);
                sum += tempInt;
            } else if ("+".equals(tempString)) {
                int tempInt = tempResult.get(tempResult.size() - 1) + tempResult.get(tempResult.size() - 2);
                tempResult.add(tempInt);
                sum += tempInt;
            } else {
                int tempInt = Integer.valueOf(tempString);
                tempResult.add(tempInt);
                sum += tempInt;
            }
        }
        return sum;
    }
}
```

# 692.前K个高频单词

```java
class Solution {
    public List<String> topKFrequent(String[] words, int k) {
        Map<String, Integer> tempMap = new HashMap<>();
        for (int i = 0; i < words.length; i++) {
            tempMap.put(words[i], tempMap.getOrDefault(words[i], 0) + 1);
        }
        String[][] tempStr = new String[tempMap.size()][2];
        int index = 0;
        for (String key : tempMap.keySet()) {
            int value = tempMap.get(key);
            tempStr[index][0] = key;
            tempStr[index][1] = String.valueOf(value);
            index++;
        }
        Arrays.sort(tempStr, new Comparator<String[]>() {
            @Override
            public int compare(String[] a, String[] b) {
                if (a[1].equals(b[1])) {
                    return a[0].compareTo(b[0]);
                } else {
                    return Integer.compare(Integer.valueOf(b[1]), Integer.valueOf(a[1]));
                }
            }
        });
        List<String> resultList = new ArrayList<>();
        int resIndex = 0;
        while (k > 0) {
            resultList.add(tempStr[resIndex++][0]);
            k--;
        }
        return resultList;
    }
}
```



# 693.交替位二进制数

```java
class Solution {
    public boolean hasAlternatingBits(int n) {
        int index = -1;
        for (int i = 31; i >= 0; i--) {
            if (((n >> i) & 1) == 1) {
                index = i;
                break;
            }
        }
        for (int i = 0; i < index; i++) {
            if (((n >> i) & 1) == ((n >> (i + 1)) & 1)) {
                return false;
            }
        }
        return true;
    }
}
```

# 695.岛屿的最大面积

```java
class Solution {

    int[][] ttGrid;
    boolean[][] visited;

    public int maxAreaOfIsland(int[][] grid) {
        int result = 0;
        ttGrid = grid;
        visited = new boolean[grid.length][grid[0].length];
        for (int i = 0; i < grid.length; i++) {
            for (int j = 0; j < grid[i].length; j++) {
                if (grid[i][j] == 1 && !visited[i][j]) {
                    int temp = findLand(i, j);
                    result = Math.max(result, temp);
                }
            }
        }
        return result;
    }
    public int findLand(int x, int y) {
        if (ttGrid[x][y] == 0 || visited[x][y]) {
            return 0;
        }
        int result = 1;
        visited[x][y] = true;
        if (x > 0) {
            result += findLand(x - 1, y);
        }
        if (y > 0) {
            result += findLand(x, y - 1);
        }
        if (x < ttGrid.length - 1) {
            result += findLand(x + 1, y);
        }
        if (y < ttGrid[0].length - 1) {
            result += findLand(x, y + 1);
        }
        return result;
    }
}
```



# 696.计数二进制子串

```java
class Solution {
    public int countBinarySubstrings(String s) {
        char[] sChar = s.toCharArray();
        List<Integer> number = new ArrayList<>();
        int cnt = 0;
        for (int i = 0; i < sChar.length - 1; i++) {
            cnt++;
            if (sChar[i] != sChar[i + 1]) {
                number.add(cnt);
                cnt = 0;
            }
            if(i == sChar.length - 2) {
                if (sChar[i] == sChar[i + 1]) {
                    number.add(cnt + 1);
                } else {
                    number.add(1);
                }
            }
        }
        int result = 0;
        for (int i = 0; i < number.size() - 1; i++) {
            result += Math.min(number.get(i), number.get(i + 1));
        }
        return result;
    }
}
```



# 697.数组的度

```java
class Solution {
    public int findShortestSubArray(int[] nums) {
        Map<Integer, List<Integer>> tempMap = new HashMap<>();
        for (int i = 0; i < nums.length; i++) {
            List<Integer> tempList = tempMap.getOrDefault(nums[i], new ArrayList<>());
            tempList.add(i);
            tempMap.put(nums[i], tempList);
        }
        int minLen = Integer.MAX_VALUE;
        int maxSize = 0;
        for (Integer key : tempMap.keySet()) {
            List<Integer> tempList = tempMap.get(key);
            if (tempList.size() > maxSize) {
                maxSize = tempList.size();
                minLen = tempList.get(tempList.size() - 1) - tempList.get(0) + 1;
            } else if (tempList.size() == maxSize) {
                minLen = Math.min(minLen, tempList.get(tempList.size() - 1) - tempList.get(0) + 1);
            }
        }
        return minLen;
    }
}
```



# 700.二叉搜索树中的搜索

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public TreeNode searchBST(TreeNode root, int val) {
        return find(root, val);
    }
    public TreeNode find(TreeNode node, int val) {
        if (node == null) {
            return null;
        }
        if (node.val == val) {
            return node;
        } else if (node.val > val) {
            return find(node.left, val);
        } else {
            return find(node.right, val);
        }
    }
}
```

### 