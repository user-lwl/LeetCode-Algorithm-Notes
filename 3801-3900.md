# 3803.统计残差前缀

```java
class Solution {
    public int residuePrefixes(String s) {
        Set<Character> tempSet = new HashSet<>();
        int result = 0;
        for (int i = 0; i < s.length(); i++) {
            tempSet.add(s.charAt(i));
            if ((i + 1) % 3 == tempSet.size()) {
                result++;
            }
        }
        return result;
    }
}
```

# 3804.中心子数组的数量

```java
class Solution {
    public int centeredSubarrays(int[] nums) {
        int result = 0;
        for (int i = 0; i < nums.length; i++) {
            int sum = 0;
            Set<Integer> tempSet = new HashSet<>();
            for (int j = i; j < nums.length; j++) {
                sum += nums[j];
                tempSet.add(nums[j]);
                if (tempSet.contains(sum)) {
                    result++;
                }
            }
        }
        return result;
    }
}
```

# 3805.统计凯撒加密对数目

```java
class Solution {
    public long countPairs(String[] words) {
        Map<String, Integer> tempMap = new HashMap<>();
        for (int i = 0; i < words.length; i++) {
            String tempStr = words[i];
            int tempIndex = tempStr.charAt(0) - 'a';
            StringBuilder sb = new StringBuilder();
            for (int j = 0; j < tempStr.length(); j++) {
                sb.append((tempStr.charAt(j) - 'a' - tempIndex + 26) % 26).append(",");
            }
            String tempResStr = sb.toString();
            tempMap.put(tempResStr, tempMap.getOrDefault(tempResStr, 0) + 1);
        }
        long result = 0;
        for (String key : tempMap.keySet()) {
            int cnt = tempMap.get(key);
            result += (long) cnt * (cnt - 1) / 2;
        }
        return result;
    }
}
```

# 3810.变成目标数组的最少操作次数

```java
class Solution {
    public int minOperations(int[] nums, int[] target) {
        Set<Integer> tempSet = new HashSet<>();
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] != target[i]) {
                tempSet.add(nums[i]);
            }
        }
        return tempSet.size();
    }
}
```



# 3813.元音辅音得分

```java
class Solution {
    public int vowelConsonantScore(String s) {
        Set<Character> yuanSet = new HashSet<>();
        yuanSet.add('a');
        yuanSet.add('e');
        yuanSet.add('i');
        yuanSet.add('o');
        yuanSet.add('u');
        int c = 0;
        int v = 0;
        for (int i = 0; i < s.length(); i++) {
            if (yuanSet.contains(s.charAt(i))) {
                v++;
            } else {
                if (Character.isLetter(s.charAt(i))) {
                    c++;
                }
            }
        }
        return c == 0 ? 0 : v / c;
    }
}
```



# 3818.移除前缀使数组严格递增

```java
class Solution {
    public int minimumPrefixLength(int[] nums) {
        for (int i = nums.length - 2; i >= 0; i--) {
            if (nums[i] >= nums[i + 1]) {
                return i + 1;
            }
        }
        return 0;
    }
}
```



# 3823.反转一个字符串里的字母后反转特殊字符

```java
class Solution {
    public String reverseByType(String s) {
        char[] sChar = s.toCharArray();
        List<Character> letterList = new ArrayList<>();
        List<Integer> letterIndexList = new ArrayList<>();
        List<Character> dotList = new ArrayList<>();
        List<Integer> dotIndexList = new ArrayList<>();
        for (int i = 0; i < sChar.length; i++) {
            if (Character.isLetter(sChar[i])) {
                letterList.add(sChar[i]);
                letterIndexList.add(i);
            } else {
                dotList.add(sChar[i]);
                dotIndexList.add(i);
            }
        }
        for (int i = 0; i < letterList.size(); i++) {
            sChar[letterIndexList.get(i)] = letterList.get(letterIndexList.size() - 1 - i);
        }
        for (int i = 0; i < dotList.size(); i++) {
            sChar[dotIndexList.get(i)] = dotList.get(dotIndexList.size() - 1 - i);
        }
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < sChar.length; i++) {
            sb.append(sChar[i]);
        }
        return sb.toString();
    }
}
```

# 3827.统计单比特整数

```java
class Solution {
    public int countMonobit(int n) {
        int result = 0;
        for (int i = 0; i <= n; i++) {
            if (i == 0 || Integer.toBinaryString(i).length() != Integer.toBinaryString(i + 1).length()) {
                result++;
            }
        }
        return result;
    }
}
```

# 3829.设计共享出行系统

```java
class RideSharingSystem {

    Deque<Integer> riderQue = new LinkedList<>();
    Deque<Integer> driverQue = new LinkedList<>();

    public RideSharingSystem() {
        
    }
    
    public void addRider(int riderId) {
        riderQue.addLast(riderId);
    }
    
    public void addDriver(int driverId) {
        driverQue.addLast(driverId);
    }
    
    public int[] matchDriverWithRider() {
        if (riderQue.isEmpty() || driverQue.isEmpty()) {
            return new int[]{-1, -1};
        }
        return new int[]{driverQue.pop(), riderQue.pop()};
    }
    
    public void cancelRider(int riderId) {
        riderQue.remove(Integer.valueOf(riderId));
    }
}

/**
 * Your RideSharingSystem object will be instantiated and called as such:
 * RideSharingSystem obj = new RideSharingSystem();
 * obj.addRider(riderId);
 * obj.addDriver(driverId);
 * int[] param_3 = obj.matchDriverWithRider();
 * obj.cancelRider(riderId);
 */
```



# 3833.统计主导元素下标数

```java
class Solution {
    public int dominantIndices(int[] nums) {
        int sum = 0;
        int result = 0;
        int cnt = nums.length;
        for (int i = 0; i < nums.length; i++) {
            sum += nums[i];
        }
        for (int i = 0; i < nums.length - 1; i++) {
            sum -= nums[i];
            cnt--;
            if (nums[i] > (double) sum / cnt) {
                result++;
            }
        }
        return result;
    }
}
```

# 3834.合并相邻且相等的元素

```java
class Solution {
    public List<Long> mergeAdjacent(int[] nums) {
        List<Long> result = new ArrayList<>();
        for (int i = 0; i < nums.length; i++) {
            result.add((long) nums[i]);
            while (result.size() >= 2 && result.get(result.size() - 1).equals(result.get(result.size() - 2))) {
                Long temp = result.get(result.size() - 1) * 2;
                result.remove(result.size() - 1);
                result.remove(result.size() - 1);
                result.add(temp);
            }
        }
        return result;
    }
}
```



# 3838.带权单词映射

```java
class Solution {
    public String mapWordWeights(String[] words, int[] weights) {
        int[] tempInt = new int[words.length];
        for (int i = 0; i < words.length; i++) {
            int sum = 0;
            for (int j = 0; j < words[i].length(); j++) {
                sum += weights[words[i].charAt(j) - 'a'];
            }
            tempInt[i] = sum % 26;
        }
        String result = "";
        for (int i = 0; i < words.length; i++) {
            String tempRes = String.valueOf((char) ('a' + 25 - tempInt[i]));
            result += tempRes;
        }
        return result;
    }
}
```

# 3839.前缀连接组的数目

```java
class Solution {
    public int prefixConnected(String[] words, int k) {
        Map<String, Integer> tempMap = new HashMap<>();
        int result = 0;
        for (int i = 0; i < words.length; i++) {
            if (words[i].length() < k) {
                continue;
            }
            String before = words[i].substring(0, k);
            int cnt = tempMap.getOrDefault(before, 0) + 1;
            tempMap.put(before, cnt);
            if (cnt == 2) {
                result++;
            }
        }
        return result;
    }
}
```



# 3842.切换打开灯泡

```java
class Solution {
    public List<Integer> toggleLightBulbs(List<Integer> bulbs) {
        Set<Integer> blubsSet = new HashSet<>();
        List<Integer> blubsList = new ArrayList<>();
        for (int i = 0; i < bulbs.size(); i++) {
            Integer blub = bulbs.get(i);
            if (!blubsSet.add(blub)) {
                blubsSet.remove(blub);
            }
        }
        for (Integer tempBlub : blubsSet) {
            blubsList.add(tempBlub);
        }
        blubsList.sort((s1, s2) -> s1.compareTo(s2));
        return blubsList;
    }
}
```

# 3843.频率唯一的第一个元素

```java
class Solution {
    public int firstUniqueFreq(int[] nums) {
        Map<Integer, Integer> tempMap = new HashMap<>();
        for (int i = 0; i < nums.length; i++) {
            tempMap.put(nums[i], tempMap.getOrDefault(nums[i], 0) + 1);
        }
        Map<Integer, Integer> cntMap = new HashMap<>();
        for (Integer key : tempMap.keySet()) {
            int value = tempMap.get(key);
            cntMap.put(value, cntMap.getOrDefault(value, 0) + 1);
        }
        for (int i = 0; i < nums.length; i++) {
            int number = nums[i];
            int pc = tempMap.get(number);
            int cnt = cntMap.get(pc);
            if (cnt == 1) {
                return number;
            }
        }
        return -1;
    }
}
```



# 3847.计算比赛分数差

```java
class Solution {
    public int scoreDifference(int[] nums) {
        int scoreA = 0;
        int scoreB = 0;
        int main = 0;
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] % 2 == 1) {
                main = 1 - main;
            }
            if ((i + 1) % 6 == 0) {
                main = 1 - main;
            }
            if (main == 0) {
                scoreA += nums[i];
            } else {
                scoreB += nums[i];
            }
        }
        return scoreA - scoreB;
    }
}
```

# 3848.阶数数字排列

```java
class Solution {
    public boolean isDigitorialPermutation(int n) {
        int jc = getJcSum(n);
        int[] tempInt = new int[10];
        while (n > 0) {
            int number = n % 10;
            tempInt[number]++;
            n /= 10;
        }
        while (jc > 0) {
            int number = jc % 10;
            tempInt[number]--;
            jc /= 10;
        }
        for (int i = 0; i < tempInt.length; i++) {
            if (tempInt[i] != 0) {
                return false;
            }
        }
        return true;
    }
    public int getJcSum(int n) {
        int result = 0;
        while (n > 0) {
            int number = n % 10;
            result += getJc(number);
            n /= 10;
        }
        return result;
    }
    public int getJc(int n) {
        int result = 1;
        while (n > 0) {
            result *= n;
            n--;
        }
        return result;
    }
}
```

# 3849.重新排列后的最大按位异或值

```java
class Solution {
    public String maximumXor(String s, String t) {
        int one = 0;
        int zero = 0;
        for (int i = 0; i < t.length(); i++) {
            if (t.charAt(i) == '1') {
                one++;
            } else {
                zero++;
            }
        }
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < s.length(); i++) {
            if (s.charAt(i) == '1') {
                if (zero > 0) {
                    sb.append(1);
                    zero--;
                } else {
                    sb.append(0);
                    one--;
                }
            } else {
                if (one > 0) {
                    sb.append(1);
                    one--;
                } else {
                    sb.append(0);
                    zero--;
                }
            }
        }
        return sb.toString();
    }
}
```



# 3852.不同频率的最小数对

```java
class Solution {
    public int[] minDistinctFreqPair(int[] nums) {
        int[] tempInt = new int[101];
        for (int i = 0; i < nums.length; i++) {
            tempInt[nums[i]]++;
        }
        List<int[]> tempList = new ArrayList<>();
        for (int i = 0; i < tempInt.length; i++) {
            if (tempInt[i] != 0) {
                tempList.add(new int[]{i, tempInt[i]});
            }
        }
        if (tempList.size() < 2) {
            return new int[]{-1, -1};
        }
        for (int i = 0; i < tempList.size(); i++) {
            for (int j = i + 1; j < tempList.size(); j++) {
                int[] a1 = tempList.get(i);
                int[] a2 = tempList.get(j);
                if (a1[1] != a2[1]) {
                    return new int[]{a1[0], a2[0]};
                }
            }
        }
        return new int[]{-1, -1};
    }
}
```



# 3856.移除尾部元音字母

```java
class Solution {
    public String trimTrailingVowels(String s) {
        for (int i = s.length() - 1; i >= 0; i--) {
            if (s.charAt(i) != 'a' && s.charAt(i) != 'e' && s.charAt(i) != 'i' && s.charAt(i) != 'o' && s.charAt(i) != 'u') {
                return s.substring(0, i + 1);
            }
        }
        return "";
    }
}
```

# 3857.拆分到1的最小总代价

```java
class Solution {
    public int minCost(int n) {
        return n * (n - 1) / 2; 
    }
}
```

# 3861.容量最小的箱子

```java
class Solution {
    public int minimumIndex(int[] capacity, int itemSize) {
        int index = -1;
        int minSize = 999;
        for (int i = 0; i < capacity.length; i++) {
            if (capacity[i] >= itemSize) {
                if (capacity[i] < minSize) {
                    index = i;
                    minSize = capacity[i];
                }
            }
        }
        return index;
    }
}
```

# 3862.找出最小平衡下标

```java
class Solution {
    public int smallestBalancedIndex(int[] nums) {
        long[] left = new long[nums.length];
        long right = 1L;
        left[0] = nums[0];
        for (int i = 1; i < nums.length; i++) {
            left[i] = nums[i] + left[i - 1];
        }
        for (int i = nums.length - 1; i >= 0; i--) {
            if (i == 0) {
                if (right == 0) {
                    return i;
                } else {
                    break;
                }
            }
            if (left[i - 1] == right) {
                return i;
            }
            right *= nums[i];
            if (right > Long.MAX_VALUE || right < 0) {
                right = -1;
            }
        }
        return -1;
    }
}
```

# 3863.将一个字符串排序的最小操作次数

```java
class Solution {
    public int minOperations(String s) {
        char[] tempStr = s.toCharArray();
        char[] tempStr2 = s.toCharArray();
        Arrays.sort(tempStr2);
        if (s.length() == 2 && (tempStr[0] != tempStr2[0] && tempStr[0] != tempStr[1])) {
            return -1;
        }
        if (s.length() == 1) {
            return 0;
        }
        for (int i = 0; i < tempStr.length; i++) {
            if (tempStr[i] != tempStr2[i]) {
                int cnt = 0;
                if (tempStr[0] != tempStr2[0]) {
                    cnt++;
                }
                if (tempStr[tempStr.length - 1] != tempStr2[tempStr.length - 1]) {
                    cnt++;
                }
                if (tempStr[0] == tempStr2[tempStr.length - 1] && tempStr2[0] == tempStr[tempStr.length - 1] && tempStr2[0] != tempStr2[1] && tempStr2[tempStr.length - 1] != tempStr2[tempStr.length - 2]) {
                    cnt++;
                }
                return cnt == 0 ? 1 : cnt;
            }
        }
        return 0;
    }
}
```

# 3866.找到第一个唯一偶数

```java
class Solution {
    public int firstUniqueEven(int[] nums) {
        Map<Integer, Integer> indexMap = new HashMap<>();
        Set<Integer> numberSet = new HashSet<>();
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] % 2 == 1) {
                continue;
            }
            if (numberSet.add(nums[i])) {
                indexMap.put(nums[i], i);
            } else {
                indexMap.remove(nums[i]);
            }
        }
        if (indexMap.size() == 0) {
            return -1;
        }
        int result = -1;
        int minIndex = 999;
        for (Integer key : indexMap.keySet()) {
            int index = indexMap.get(key);
            if (index < minIndex) {
                minIndex = index;
                result = key;
            }
        }
        return result;
    }
}
```

# 3868.通过交换使数组相等的最小花费

```java
class Solution {
    public int minCost(int[] nums1, int[] nums2) {
        Map<Integer, Integer> map1 = new HashMap<>();
        Map<Integer, Integer> map2 = new HashMap<>();
        Set<Integer> allNumSet = new HashSet<>();
        for (int i = 0; i < nums1.length; i++) {
            map1.put(nums1[i], map1.getOrDefault(nums1[i], 0) + 1);
            map2.put(nums2[i], map2.getOrDefault(nums2[i], 0) + 1);
            allNumSet.add(nums1[i]);
            allNumSet.add(nums2[i]);
        }
        int result = 0;
        for (Integer key : allNumSet) {
            int cnt1 = map1.getOrDefault(key, 0);
            int cnt2 = map2.getOrDefault(key, 0);
            if ((cnt1 + cnt2) % 2 == 1) {
                return -1;
            }
            result += Math.abs(cnt1 - cnt2) / 2;
        }
        return result / 2;
    }
}
```

# 3870.统计范围内的逗号

```java
class Solution {
    public int countCommas(int n) {
        return n - 999 < 0 ? 0 : n - 999;
    }
}
```

# 3871.统计范围内的逗号Ⅱ

```java
class Solution {
    public long countCommas(long n) {
        // 1,000,000,000,000,000
        long cnt1 = n - 999L < 0 ? 0 : n - 999L;
        long cnt2 = n - 999999L < 0 ? 0 : n - 999999L;
        long cnt3 = n - 999999999L < 0 ? 0 : n - 999999999L;
        long cnt4 = n - 999999999999L < 0 ? 0 : n - 999999999999L;
        long cnt5 = n - 999999999999999L < 0 ? 0 : n - 999999999999999L;
        return cnt1 + cnt2 + cnt3 + cnt4 + cnt5;
    }
}
```

# 3875.构造奇偶一致的数组Ⅰ

```java
class Solution {
    public boolean uniformArray(int[] nums1) {
        return true;
    }
}
```

# 3876.构造奇偶一致的数组Ⅱ

```java
class Solution {
    public boolean uniformArray(int[] nums1) {
        int minOu = Integer.MAX_VALUE;
        int minJi = Integer.MAX_VALUE;
        for (int i = 0; i < nums1.length; i++) {
            if (nums1[i] % 2 == 0) {
                minOu = Math.min(minOu, nums1[i]);
            } else {
                minJi = Math.min(minJi, nums1[i]);
            }
        }
        if (minOu == Integer.MAX_VALUE || minJi == Integer.MAX_VALUE) {
            return true;
        }
        if (minOu < minJi) {
            // 变偶数
            return false;
        } else {
            // 变奇数
            return true;
        }
    }
}
```

# 3877.达到目标异或值的最少删除次数

```java
class Solution {

    int[] totalNums;
    
    public int minRemovals(int[] nums, int target) {
        int result = -1;
        totalNums = nums;
        Map<Integer, Integer> leftMap = new HashMap<>();
        Map<Integer, Integer> rightMap = new HashMap<>();
        // 左
        doHelper(0, 0, 0, nums.length / 2, leftMap);
        // 右
        doHelper(nums.length / 2 + 1, 0, 0, nums.length - 1, rightMap);
        for (Integer key : leftMap.keySet()) {
            int leftCnt = leftMap.get(key);
            int needNumber = target ^ key;
            if (rightMap.containsKey(needNumber)) {
                int rightCnt = rightMap.get(needNumber);
                result = Math.max(result, leftCnt + rightCnt);
            }
        }
        return result == -1 ? result : nums.length - result;
    }
    public void doHelper(int index, int xorSum, int keepCnt, int endIndex, Map<Integer, Integer> map) {
        if (index > endIndex){
            map.put(xorSum, Math.max(map.getOrDefault(xorSum, 0), keepCnt));
            return;
        }
        // 不用
        doHelper(index + 1, xorSum, keepCnt, endIndex, map);
        // 用
        doHelper(index + 1, xorSum ^ totalNums[index], keepCnt + 1, endIndex, map);
    }
}
```

# 3880.两个值之间的最小绝对差值

```java
class Solution {
    public int minAbsoluteDifference(int[] nums) {
        List<Integer> oneList = new ArrayList<>();
        List<Integer> twoList = new ArrayList<>();
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] == 1) {
                oneList.add(i);
            } else if (nums[i] == 2) {
                twoList.add(i);
            }
        }
        int result = Integer.MAX_VALUE;
        for (int i = 0; i < oneList.size(); i++) {
            for (int j = 0; j < twoList.size(); j++) {
                int tempRes = Math.abs(oneList.get(i) - twoList.get(j));
                result = Math.min(result, tempRes);
            }
        }
        return result == Integer.MAX_VALUE ? -1 : result;
    }
}
```

# 3881.恰好看到K个人的方向选择

```java
class Solution {

    int mod = (int) 1e9 + 7;
    
    public int countVisiblePeople(int n, int pos, int k) {
        // n-1 中选择 k
        // C(n, k) = n * ... * (n - k) + 1 / k!
        // (a / b) % mod = a * b^(-1) % mod
        k = Math.min(n - 1 - k, k);
        long result = doJie(n - k, n - 1, k);
        return (int) ((result * 2) % mod);
    }
    public long doJie(int start, int end, int k) {
        long result = 1;
        for (int i = start; i <= end; i++) {
            result *= i;
            result %= mod;
            long temp = (long) (k - i + start);
            long ivkTmp = ivk(temp);
            result *= ivkTmp;
            result %= mod;
        }
        return result;
    }
    // 乘法逆元代替除法
    public long ivk(long a) {
        return pow(a, mod - 2);
    }
    public long pow(long a, long b) {
        long result = 1;
        a %= mod;
        while (b > 0) {
            if ((b & 1) == 1) {
                result *= a;
                result %= mod;
            }
            a *= a;
            a %= mod;
            b >>= 1;
        }
        return result;
    }
}
```

# 3882.网格图中最小异或路径

```java
class Solution {
    public int minCost(int[][] grid) {
        Set<Integer>[][] dp = new Set[grid.length][grid[0].length];
        dp[0][0] = new HashSet<>();
        dp[0][0].add(grid[0][0]);
        for (int i = 1; i < grid.length; i++) {
            dp[i][0] = new HashSet<>();
            for (Integer key : dp[i - 1][0]) {
                dp[i][0].add(key ^ grid[i][0]);
            }
        }
        for (int i = 1; i < grid[0].length; i++) {
            dp[0][i] = new HashSet<>();
            for (Integer key : dp[0][i - 1]) {
                dp[0][i].add(key ^ grid[0][i]);
            }
        }
        for (int i = 1; i < grid.length; i++) {
            for (int j = 1; j < grid[0].length; j++) {
                dp[i][j] = new HashSet<>();
                for (Integer key : dp[i][j - 1]) {
                    dp[i][j].add(key ^ grid[i][j]);
                }
                for (Integer key : dp[i - 1][j]) {
                    dp[i][j].add(key ^ grid[i][j]);
                }
            }
        }
        int result = Integer.MAX_VALUE;
        for (Integer key : dp[dp.length - 1][dp[0].length - 1]) {
            result = Math.min(result, key);
        }
        return result;
    }
}
```

# 3884.双端字符匹配

```java
class Solution {
    public int firstMatchingIndex(String s) {
        char[] tempChar = s.toCharArray();
        for (int i = 0; i < tempChar.length; i++) {
            if (tempChar[i] == tempChar[tempChar.length - i - 1]) {
                return i;
            }
        }
        return -1;
    }
}
```

# 3885.设计事件管理器

```java
class EventManager {

    TreeSet<int[]> ts = new TreeSet<>((a, b) -> {
        if (a[1] == b[1]) {
            return Integer.compare(a[0], b[0]);
        } else {
            return Integer.compare(b[1], a[1]);
        }
    });
    Map<Integer, Integer> map = new HashMap<>();

    public EventManager(int[][] events) {
        for (int i = 0; i < events.length; i++) {
            ts.add(new int[]{events[i][0], events[i][1]});
            map.put(events[i][0], events[i][1]);
        }
    }
    
    public void updatePriority(int eventId, int newPriority) {
        int oldPriority = map.get(eventId);
        ts.remove(new int[]{eventId, oldPriority});
        ts.add(new int[]{eventId, newPriority});
        map.put(eventId, newPriority);
    }
    
    public int pollHighest() {
        if (ts.size() == 0) {
            return -1;
        }
        int[] result = ts.pollFirst();
        return result[0];
    }
}

/**
 * Your EventManager object will be instantiated and called as such:
 * EventManager obj = new EventManager(events);
 * obj.updatePriority(eventId,newPriority);
 * int param_2 = obj.pollHighest();
 */
```

# 3886.可排序整数求和

```java
class Solution {
    public int sortableIntegers(int[] nums) {
        int result = 0;
        int n = nums.length;
        int[] numsSort = new int[n];
        System.arraycopy(nums, 0, numsSort, 0, n);
        Arrays.sort(numsSort);
        // String str = Arrays.toString(numsSort);
        next:
        for (int i = 1; i <= n; i++) {
            if (n % i != 0) {
                continue;
            }
            int[] numsTemp = new int[n];
            for (int j = 0; j < n; j += i) {
                // int[] temp = new int[i * 2];
                // int[] tempSort = new int[i];
                // System.arraycopy(nums, j, temp, 0, i);
                // System.arraycopy(nums, j, temp, i, i);
                // System.arraycopy(numsSort, j, tempSort, 0, i);
                // String tempStr = Arrays.toString(temp);
                // String tempStrSort = Arrays.toString(tempSort);
                // String tempStrSplit = tempStr.substring(1, tempStr.length() - 1);
                // String tempStrSortSplit = tempStrSort.substring(1, tempStrSort.length() - 1);
                // if (tempStrSplit.indexOf(tempStrSortSplit) == -1) {
                //     continue next;
                // }
                int[] temp = new int[i * 2];
                int[] tempSort = new int[i];
                System.arraycopy(nums, j, temp, 0, i);
                System.arraycopy(nums, j, temp, i, i);
                System.arraycopy(numsSort, j, tempSort, 0, i);
                if (!isEqu(temp, tempSort)) {
                    continue next;
                }
            }
            result += i;
        }
        return result;
    }
    public boolean isEqu(int[] temp, int[] tempSort) {
        for (int i = 0; i < temp.length; i++) {
            int cha = i;
            while (i < temp.length && temp[i] == tempSort[i - cha]) {
                i++;
                if (i - cha == tempSort.length) {
                    return true;
                }
            }
        }
        return false;
    }
}
```

# 3889.镜像频次距离

```java
class Solution {
    public int mirrorFrequency(String s) {
        int[] diag = new int[26];
        int[] nums = new int[10];
        for (int i = 0; i < s.length(); i++) {
            char temp = s.charAt(i);
            if (temp >= '0' && temp <= '9') {
                nums[temp - '0']++;
            } else {
                diag[temp - 'a']++;
            }
        }
        int result = 0;
        for (int i = 0; i <= 12; i++) {
            result += Math.abs(diag[i] - diag[25 - i]);
        }
        for (int i = 0; i <= 4; i++) {
            result += Math.abs(nums[i] - nums[9 - i]);
        }
        return result;
    }
}
```

# 3890.可由多种立方和构造的整数

```java
class Solution {
    public List<Integer> findGoodIntegers(int n) {
        List<Integer> resultList = new ArrayList<>();
        Set<Integer> tempSet = new HashSet<>();
        Set<Integer> haveSet = new HashSet<>();
        for (int i = 0; i * i * i <= n; i++) {
            for (int j = i; i * i * i + j * j * j <= n; j++) {
                int tmp = i * i * i + j * j * j;
                if (!tempSet.add(tmp)) {
                    if (haveSet.add(tmp)) {
                        resultList.add(tmp);
                    }
                }
            }
        }
        Collections.sort(resultList);
        return resultList;
    }
}
```

# 3891.最大化特殊下标数目的最少增加次数

```java
class Solution {
    public long minIncrease(int[] nums) {
        long result1 = 0;
        int n = nums.length;
        if (n <= 2) {
            return 0;
        }
        // 奇数
        if (n % 2 == 1) {
            for (int i = 1; i < n - 1; i += 2) {
                result1 += Math.max(Math.max(nums[i - 1], nums[i + 1]) - nums[i] + 1, 0);
            }
            return result1;
        }
        // 花费 - 偶数使用
        long[] cost = new long[n];
        long res2 = 0;
        long ji = 0;
        long ou = 0;
        for (int i = 1; i < n - 1; i++) {
            // 计算花费
            cost[i] = Math.max(Math.max(nums[i - 1], nums[i + 1]) - nums[i] + 1, 0);
            if (i % 2 == 0) {
                res2 += cost[i];
                ou += cost[i];
            } else {
                ji += cost[i];
            }
        }
        long res1 = 0;
        long minRes1 = Long.MAX_VALUE;
        for (int i = 1; i < n - 1; i+=2) {
            res1 += cost[i];
            ji -= cost[i];
            ou -= cost[i + 1];
            minRes1 = Math.min(minRes1, res1 + Math.min(ji, ou));
        }
        return Math.min(minRes1, res2);
    }
}
```

# 3894.交通信号灯的颜色

```java
class Solution {
    public String trafficSignal(int timer) {
        if (timer == 0) {
            return "Green";
        } else if (timer == 30) {
            return "Orange";
        } else if (timer > 30 && timer <= 90) {
            return "Red";
        } else {
            return "Invalid";
        }
    }
}
```

# 3895.统计数字出现总次数

```java
class Solution {
    public int countDigitOccurrences(int[] nums, int digit) {
        int result = 0;
        for (int i = 0; i < nums.length; i++) {
            int number = nums[i];
            while (number > 0) {
                int temp = number % 10;
                number /= 10;
                if (temp == digit) {
                    result++;
                }
            }
        }
        return result;
    }
}
```

# 3896.将数组转换为交替质数数组的最少操作次数

```java
class Solution {
    public int minOperations(int[] nums) {
        int max = 0;
        for (int i = 0; i < nums.length; i++) {
            max = Math.max(max, nums[i]);
        }
        int limit = max + 200;
        boolean[] isNoZhi = new boolean[limit];
        isNoZhi[0] = true;
        isNoZhi[1] = true;
        for (int i = 2; i * i < isNoZhi.length; i++) {
            if (!isNoZhi[i]) {
                for (int j = i + i; j < isNoZhi.length; j+=i) {
                    isNoZhi[j] = true;
                }
            }
        }
        int[] zhi = new int[limit];
        int[] noZhi = new int[limit];
        int maxZhi = Integer.MAX_VALUE;
        for (int i = limit - 1; i > 2; i--) {
            if (!isNoZhi[i]) {
                zhi[i] = i;
                maxZhi = zhi[i];
                noZhi[i] = i + 1;
            } else {
                zhi[i] = maxZhi;
                noZhi[i] = i;
            }
        }
        zhi[2] = 2;
        zhi[1] = 2;
        noZhi[2] = 4;
        noZhi[1] = 1;
        int result = 0;
        for (int i = 0; i < nums.length; i++) {
            if (i % 2 == 0) {
                // 质数
                result += zhi[nums[i]] - nums[i];
            } else {
                result += noZhi[nums[i]] - nums[i];
            }
        }
        return result;
    }
}
```

# 3897.连接二进制片段得到的最大值

```java
class Solution {
    int mod = (int) 1e9 + 7;
    public int maxValue(int[] nums1, int[] nums0) {
        int[][] number = new int[nums1.length][2];
        for (int i = 0; i < nums1.length; i++) {
            number[i][0] = nums0[i];
            number[i][1] = nums1[i];
        }
        Arrays.sort(number, (a, b) -> {
            if (a[0] == b[0]) {
                return b[1] - a[1];
            } else if (a[0] == 0) {
                return -1;
            } else if (b[0] == 0) {
                return 1;
            }
            if (a[1] == b[1]) {
                return a[0] - b[0];
            }
            return b[1] - a[1];
        });
        long result = 0;
        for (int i = 0; i < number.length; i++) {
            int len = number[i][0] + number[i][1];
            long temp = getValue(number[i][1], number[i][0]);
            result = ((result * powMod(2, len) % mod) + temp) % mod;
        }
        return (int) result;
    }
    public long getValue(int one, int zero) {
        long part1 = (powMod(2, one) - 1 + mod) % mod;
        long part2 = powMod(2, zero);
        return part1 * part2 % mod;
    }
    public long powMod(long a, long b) {
        long result = 1;
        while (b > 0) {
            if ((b & 1) == 1) {
                result = result * a % mod;
            }
            a = a * a % mod;
            b = b /= 2;
        }
        return result;
    }
}
```

# 3898.统计每个顶点的度

```java
class Solution {
    public int[] findDegrees(int[][] matrix) {
        int[] result = new int[matrix.length];
        for (int i = 0; i < matrix.length; i++) {
            for (int j = 0; j < matrix[i].length; j++) {
                result[i] += matrix[i][j];
            }
        }
        return result;
    }
}
```

# 3899.三角形的内角度数

```java
class Solution {
    public double[] internalAngles(int[] sides) {
        Arrays.sort(sides);
        if (sides[0] + sides[1] <= sides[2]) {
            return new double[]{};
        }
        int a = sides[0];
        int b = sides[1];
        int c = sides[2];
        double cosA = (double) (b * b + c * c - a * a) / (2 * b * c);
        double cosB = (double) (a * a + c * c - b * b) / (2 * a * c);
        double aa = Math.toDegrees(Math.acos(cosA));
        double bb = Math.toDegrees(Math.acos(cosB));
        double cc = (double) 180 - aa - bb;
        return new double[]{aa, bb, cc};
    }
}
```

# 3900.一次交换后的最长平衡子串

```java
class Solution {
    int[] prefix;
    public int longestBalanced(String s) {
        prefix = new int[s.length() + 1];
        for (int i = 1; i < prefix.length; i++) {
            prefix[i] = prefix[i - 1] + s.charAt(i - 1) - '0';
        }
        int result = 0;
        Map<Integer, List<Integer>> tempMap = new HashMap<>();
        for (int i = 0; i < prefix.length; i++) {
            int len = i;
            int one = prefix[i];
            int zero = len - one;
            int cha = one - zero;
            // 0
            if (tempMap.containsKey(cha)) {
                List<Integer> tempList = tempMap.get(cha);
                for (Integer index : tempList) {
                    int tempRes = i - index;
                    if (tempRes < result) {
                        break;
                    }
                    result = Math.max(result, tempRes);
                }
                
            }
            // 2   1>0
            if (tempMap.containsKey(cha - 2)) {
                List<Integer> tempList = tempMap.get(cha - 2);
                for (Integer index : tempList) {
                    int tempRes = i - index;
                    if (tempRes < result) {
                        break;
                    }
                    if (haveZero(index, i)) {
                        result = Math.max(result, tempRes);
                    }
                }
            }
            // -2   0>1
            if (tempMap.containsKey(cha + 2)) {
                List<Integer> tempList = tempMap.get(cha + 2);
                for (Integer index : tempList) {
                    int tempRes = i - index;
                    if (tempRes < result) {
                        break;
                    }
                    if (haveOne(index, i)) {
                        result = Math.max(result, tempRes);
                    }
                }
            }
            // add
            List<Integer> tempListNew = tempMap.getOrDefault(cha, null);
            if (tempListNew == null) {
                tempListNew = new ArrayList();
                tempMap.put(cha, tempListNew);
            }
            tempListNew.add(i);
        }
        return result;
    }
    public boolean haveOne(int left, int right) {
        int leftOne = prefix[left];
        if (leftOne > 0) {
            return true;
        }
        int rightOne = prefix[prefix.length - 1] - prefix[right];
        if (rightOne > 0) {
            return true;
        }
        return false;
    }
    public boolean haveZero(int left, int right) {
        int leftOne = prefix[left];
        int leftZero = left - leftOne;
        if (leftZero > 0) {
            return true;
        }
        int rightOne = prefix[prefix.length - 1] - prefix[right];
        int rightLen = prefix.length - 1 - right;
        int rightZero = rightLen - rightOne;
        if (rightZero > 0) {
            return true;
        }
        return false;
    }
}
```

