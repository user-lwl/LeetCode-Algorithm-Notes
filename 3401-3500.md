# 3402.使每一列严格递增的最少操作次数

```java
class Solution {
    public int minimumOperations(int[][] grid) {
        int result = 0;
        int[] maxList = new int[grid[0].length];
        for (int i = 0; i < grid[0].length; i++) {
            maxList[i] = grid[0][i];
        }
        for (int i = 1; i < grid.length; i++) {
            for (int j = 0; j < grid[0].length; j++) {
                if (grid[i][j] > maxList[j]) {
                    maxList[j] = grid[i][j];
                } else {
                    result += maxList[j] - grid[i][j] + 1;
                    maxList[j]++;
                }
            }
        }
        return result;
    }
}
```

# 3438.找到字符串中合法的相邻数字

```java
class Solution {
    public String findValidPair(String s) {
        int[] cnt = new int[10];
        for (int i = 0; i < s.length(); i++) {
            cnt[s.charAt(i) - '0']++;
        }
        for (int i = 0; i < s.length() - 1; i++) {
            int temp1 = s.charAt(i) - '0';
            int temp2 = s.charAt(i + 1) - '0';
            if (temp1 != temp2 && cnt[temp1] == temp1 && cnt[temp2] == temp2) {
                return "" + temp1 + temp2;
            }
        }
        return "";
    }
}
```



# 3442.奇偶频次间的最大差值Ⅰ

```java
class Solution {
    public int maxDifference(String s) {
        int[] cnt = new int[26];
        for (int i = 0; i < s.length(); i++) {
            char tempChar = s.charAt(i);
            cnt[tempChar - 'a']++;
        }
        int maxJi = 0;
        int minOu = 999;
        for (int i = 0; i < cnt.length; i++) {
            if (cnt[i] == 0) {
                continue;
            }
            if (cnt[i] % 2 == 0) {
                minOu = Math.min(minOu, cnt[i]);
            } else {
                maxJi = Math.max(maxJi, cnt[i]);
            }
        }
        return maxJi - minOu;
    }
}
```



# 3467.将数组按照奇偶性转化

```java
class Solution {
    public int[] transformArray(int[] nums) {
        for (int i = 0; i < nums.length; i++) {
            nums[i] = nums[i] % 2;
        }
        Arrays.sort(nums);
        return nums;
    }
}
```

# 3483.不同三位偶数的数目

```java
class Solution {
    public int totalNumbers(int[] digits) {
        Set<Integer> resultSet = new HashSet<>();
        for (int i = 0; i < digits.length; i++) {
            for (int j = 0; j < digits.length; j++) {
                for (int k = 0; k < digits.length; k++) {
                    if (i != j && i != k && j != k && digits[k] % 2 == 0 && digits[i] != 0) {
                        int number = digits[i] * 100 + digits[j] * 10 + digits[k];
                        resultSet.add(number);
                    }
                }
            }
        }
        return resultSet.size();
    }
}
```



# 3487.删除后的最大子数组元素和

```java
class Solution {
    public int maxSum(int[] nums) {
        Set<Integer> resultSet = new HashSet<>();
        int sum = 0;
        int count = 0;
        int max = Integer.MIN_VALUE;
        for (int i = 0; i < nums.length; i++) {
            if (resultSet.add(nums[i]) && nums[i] >= 0) {
                sum += nums[i];
                count++;
            }
            max = Math.max(max, nums[i]);
        }
        return count == 0 ? max : sum;
    }
}
```

