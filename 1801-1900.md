# 1805.字符串中不同整数的数目

```java
class Solution {
    public int numDifferentIntegers(String word) {
        Set<String> numberSet = new HashSet<>();
        for (int i = 0; i < word.length(); i++) {
            char temp = word.charAt(i);
            if (Character.isDigit(temp) ) {
                int startIndex = i;
                while (i < word.length() && Character.isDigit(word.charAt(i))) {
                    i++;
                }
                while (startIndex < i && word.charAt(startIndex) == '0') {
                    startIndex++;
                }
                String tempResult = word.substring(startIndex, i);
                numberSet.add(tempResult);
            }
        }
        return numberSet.size();
    }
}
```

# 1807.替换字符串中的括号内容

```java
class Solution {
    public String evaluate(String s, List<List<String>> knowledge) {
        Map<String, String> tempMap = new HashMap<>();
        for (int i = 0; i < knowledge.size(); i++) {
            List<String> tempList = knowledge.get(i);
            tempMap.put(tempList.get(0), tempList.get(1));
        }
        StringBuilder sb = new StringBuilder();
        StringBuilder tempSb= new StringBuilder();
        boolean flag = false;
        for (int i = 0; i < s.length(); i++) {
            if (s.charAt(i) == '(') {
                flag = true;
            } else if (s.charAt(i) == ')') {
                flag = false;
                String temp = tempMap.getOrDefault(tempSb.toString(), "?");
                sb.append(temp);
                tempSb = new StringBuilder();
            } else if (!flag) {
                sb.append(s.charAt(i));
            } else if (flag) {
                tempSb.append(s.charAt(i));
            }
        }
        return sb.toString();
    }
}
```



# 1812.判断国际象棋棋盘中一个格子的颜色

```java
class Solution {
    public boolean squareIsWhite(String coordinates) {
        int code = coordinates.charAt(0) - 'a';
        int number = coordinates.charAt(1) - '0';
        return (code + number) % 2 == 0;
    }
}
```

# 1814.统计一个数组中好对子的数目

```java
class Solution {
    public int countNicePairs(int[] nums) {
        Map<Integer, Integer> tempMap = new HashMap<>();
        for (int i = 0; i < nums.length; i++) {
            int number = nums[i];
            StringBuilder sb = new StringBuilder();
            sb.append(number);
            sb.reverse();
            int revNumber = Integer.valueOf(sb.toString());
            int tempRes = number - revNumber;
            tempMap.put(tempRes, tempMap.getOrDefault(tempRes, 0) + 1);
        }
        int mod = (int) 1e9 + 7;
        long result = 0;
        for (Integer key : tempMap.keySet()) {
            int cnt = tempMap.get(key);
            result += (long) cnt * (cnt - 1) / 2;
            result %= mod;
        }
        return (int) result;
    }
}
```



# 1816.截断句子

```java
class Solution {
    public String truncateSentence(String s, int k) {
        String[] word = s.split(" ");
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < k; i++) {
            sb.append(word[i]);
            if (i < k - 1) {
                sb.append(' ');
            }
        }
        return sb.toString();
    }
}
```

# 1817.查找用户活跃分钟数

```java
class Solution {
    public int[] findingUsersActiveMinutes(int[][] logs, int k) {
        Map<Integer, Set<Integer>> resultMap = new HashMap<>();
        int[] result = new int[k];
        for (int i = 0; i < logs.length; i++) {
            int user = logs[i][0];
            int minute = logs[i][1];
            Set<Integer> tempSet = resultMap.get(user);
            if (tempSet == null) {
                tempSet = new HashSet<>();
                resultMap.put(user, tempSet);
            }
            tempSet.add(minute);
        }
        for (Integer key : resultMap.keySet()) {
            Set<Integer> tempSet = resultMap.get(key);
            int cnt = tempSet.size();
            result[cnt - 1]++;
        }
        return result;
    }
}
```



# 1827.最少操作使数组递增

```java
class Solution {
    public int minOperations(int[] nums) {
        int len = nums.length;
        int result = 0;
        for (int i = 0; i < len - 1; i++) {
            if (nums[i] >= nums[i + 1]) {
                result += nums[i] - nums[i + 1] + 1;
                nums[i + 1] = nums[i] + 1;
            }
        }
        return result;
    }
}
```

# 1832.判断句子是否为全字母句

```java
class Solution {
    public boolean checkIfPangram(String sentence) {
        int[] resultInt = new int[26];
        for (int i = 0; i < sentence.length(); i++) {
            resultInt[sentence.charAt(i) - 'a']++;
        }
        for (int i = 0; i < resultInt.length; i++) {
            if (resultInt[i] == 0) {
                return false;
            }
        }
        return true;
    }
}
```



# 1833.雪糕的最大数量

```java
class Solution {
    public int maxIceCream(int[] costs, int coins) {
        int[] counting = new int[100001];
        for (int i = 0; i < costs.length; i++) {
            counting[costs[i]]++;
        }
        int count = 0;
        for (int i = 0; i < counting.length; i++) {
            while (counting[i] > 0) {
                coins -= i;
                if (coins >= 0) {
                    count++;
                    counting[i]--;
                } else {
                    break;
                }
            }
            if (coins <= 0) {
                break;
            }
        }
        return count;
    }
}
```

# 1837.K进制表示下的各位数字总和

```java
class Solution {
    public int sumBase(int n, int k) {
        int result = 0;
        while (n > 0) {
            result += n % k;
            n /= k;
        }
        return result;
    }
}
```



# 1844.将所有数字用字符替换

```java
class Solution {
    public String replaceDigits(String s) {
        StringBuilder sb = new StringBuilder();
        sb.append(s.charAt(0));
        for (int i = 1; i < s.length(); i+=2) {
            sb.append(shift(s.charAt(i - 1), s.charAt(i) - '0'));
            if (i + 1 < s.length()) {
                sb.append(s.charAt(i + 1));
            }
        }
        return sb.toString();
    }
    public char shift(char tempChar, int tempInt) {
        return (char) (tempChar + tempInt);
    }
}
```

# 1848.到目标元素的最小距离

```java
class Solution {
    public int getMinDistance(int[] nums, int target, int start) {
        int result = 9999;
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] == target) {
                result = Math.min(result, Math.abs(i - start));
            }
        }
        return result;
    }
}
```



# 1854.人口最多的年份

```java
class Solution {
    public int maximumPopulation(int[][] logs) {
        int[] people = new int[101];
        for (int i = 0; i < logs.length; i++) {
            for (int j = logs[i][0]; j < logs[i][1]; j++) {
                people[j - 1950]++;
            }
        }
        int result = 0;
        int max = 0;
        for (int i = 0; i < people.length; i++) {
            if (people[i] > max) {
                result = 1950 + i;
                max = people[i];
            }
        }
        return result;
    }
}
```



# 1859.将句子排序

```java
class Solution {
    public String sortSentence(String s) {
        String[] splitString = s.split(" ");
        Map<Integer, String> resultMap = new HashMap<>();
        StringBuilder sb = new StringBuilder();
        for (String tempString : splitString) {
            String word = tempString.substring(0, tempString.length() - 1);
            Integer index = Integer.valueOf(tempString.substring(tempString.length() - 1));
            resultMap.put(index, word);
        }
        for (int i = 1; i < 10; i++) {
            String tempString = resultMap.getOrDefault(i, null);
            if (tempString == null) {
                return sb.toString();
            }
            if (!sb.isEmpty()) {
                sb.append(" ");
            }
            sb.append(tempString);
        }
        return sb.toString();
    }
}
```



# 1863.找出所有子集的异或总和再求和

```java
class Solution {
    public int subsetXORSum(int[] nums) {
        int sum = 0;
        int len = nums.length;
        for (int i = 0; i < (1<<len); i++) {
            int result = 0;
            for (int j = 0; j < len; j++) {
                if ((i & (1<<j)) != 0) {
                    result ^= nums[j];
                }
            }
            sum += result;
        }
        return sum;
    }
}
```

# 1869.哪种连续子字符串更长

```java
class Solution {
    public boolean checkZeroOnes(String s) {
        int oneLen = 0;
        int zeroLen = 0;
        for (int i = 0; i < s.length(); i++) {
            char tempChar = s.charAt(i);
            int startIndex = i;
            while (i < s.length() && s.charAt(i) == tempChar) {
                i++;
            }
            if (tempChar == '0') {
                zeroLen = Math.max(zeroLen, i - startIndex);
            } else {
                oneLen = Math.max(oneLen, i - startIndex);
            }
            i--;
        }
        return oneLen > zeroLen;
    }
}
```



# 1876.长度为三且各字符不同的子字符串

```java
class Solution {
    public int countGoodSubstrings(String s) {
        char[] c = s.toCharArray();
        int number = 0;
        for (int i = 0; i < c.length - 2; i++) {
            if (c[i] != c[i + 1] && c[i] != c[i + 2] && c[i + 1] != c[i + 2]) {
                number++;
            }
        }
        return number;
    }
}
```

# 1880.检查某单词是否等于两单词之和

```java
class Solution {
    public boolean isSumEqual(String firstWord, String secondWord, String targetWord) {
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < firstWord.length(); i++) {
            sb.append(firstWord.charAt(i) - 'a');
        }
        int firstInt = Integer.valueOf(sb.toString());
        sb = new StringBuilder();
        for (int i = 0; i < secondWord.length(); i++) {
            sb.append(secondWord.charAt(i) - 'a');
        }
        int secondInt = Integer.valueOf(sb.toString());
        sb = new StringBuilder();
        for (int i = 0; i < targetWord.length(); i++) {
            sb.append(targetWord.charAt(i) - 'a');
        }
        int targetInt = Integer.valueOf(sb.toString());
        return firstInt + secondInt == targetInt;
    }
}
```

# 1886.判断矩阵经轮转后是否一致

```java
class Solution {
    public boolean findRotation(int[][] mat, int[][] target) {
        boolean zero = true;
        boolean nineTen = true;
        boolean oneEightTen = true;
        boolean twoSevenTen = true;
        for (int i = 0; i < mat.length; i++) {
            for (int j = 0; j < mat.length; j++) {
                // 0
                if (mat[i][j] != target[i][j]) {
                    zero = false;
                }
                // 90
                if (mat[i][j] != target[j][mat.length - 1 - i]) {
                    nineTen = false;
                }
                // 180
                if (mat[i][j] != target[mat.length - 1 - i][mat.length - 1 - j]) {
                    oneEightTen = false;
                }
                // 270
                if (mat[i][j] != target[mat.length - 1 - j][i]) {
                    twoSevenTen = false;
                }
            }
        }
        return zero || oneEightTen || twoSevenTen || nineTen;
    }
}
```

# 1887.使数组元素相等的减少操作次数

```java
class Solution {
    public int reductionOperations(int[] nums) {
        Arrays.sort(nums);
        int cnt = 0;
        int result = 0;
        for (int i = 1; i < nums.length; i++) {
            if (nums[i] > nums[i - 1]) {
                cnt++;
            }
            result += cnt;
        }
        return result;
    }
}
```



# 1890.2020年最后一次登录

```sql
# Write your MySQL query statement below
select user_id, max(time_stamp) last_stamp from Logins where time_stamp >= '2020-01-01 00:00:00' and time_stamp <= '2020-12-31 23:59:59' group by user_id;
```



# 1893.检查是否区域内所有整数都被覆盖

```java
class Solution {
    public boolean isCovered(int[][] ranges, int left, int right) {
        int[] result = new int[51];
        for (int i = 0; i < ranges.length; i++) {
            for (int j = ranges[i][0]; j <= ranges[i][1]; j++) {
                result[j]++;
            }
        }
        for (int i = left; i <= right; i++) {
            if (result[i] == 0) {
                return false;
            }
        }
        return true;
    }
}
```

# 1897.重新分配字符串使所有字符串都相等

```java
class Solution {
    public boolean makeEqual(String[] words) {
        int[] tempList = new int[26];
        int n = words.length;
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < words[i].length(); j++) {
                tempList[words[i].charAt(j) - 'a']++;
            }
        }
        for (int i = 0; i < tempList.length; i++) {
            if (tempList[i] % n != 0) {
                return false;
            }
        }
        return true;
    }
}
```

