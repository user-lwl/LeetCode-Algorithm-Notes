# 2201.统计可以提取的工件

```java
class Solution {
    public int digArtifacts(int n, int[][] artifacts, int[][] dig) {
        Set<String> tempSet = new HashSet<>();
        for (int i = 0; i < dig.length; i++) {
            String tempString = dig[i][0] + "," + dig[i][1];
            tempSet.add(tempString);
        }
        int result = 0;
        for (int i = 0; i < artifacts.length; i++) {
            boolean flag = true;
            for (int m = artifacts[i][0]; m <= artifacts[i][2]; m++) {
                for (int nn = artifacts[i][1]; nn <= artifacts[i][3]; nn++) {
                    if (!tempSet.contains(m + "," + nn)) {
                        flag = false;
                    }
                }
            }
            if (flag) {
                result++;
            }
        }
        return result;
    }
}
```



# 2206.将数组划分成相等数对

```java
class Solution {
    public boolean divideArray(int[] nums) {
        Set<Integer> tempSet = new HashSet<>();
        for (int i = 0; i < nums.length; i++) {
            if (!tempSet.add(nums[i])) {
                tempSet.remove(nums[i]);
            }
        }
        return tempSet.size() == 0;
    }
}
```



# 2210.统计数组中峰和谷的数量

```java
class Solution {
    public int countHillValley(int[] nums) {
        int result = 0;
        int preNumber = nums[0];
        int thisNumber = nums[1];
        int nextNumber = nums[2];
        for (int i = 2; i < nums.length - 1; i++) {
            if ((thisNumber > preNumber && thisNumber > nextNumber) || (thisNumber < preNumber && thisNumber < nextNumber)) {
                result++;
                preNumber = nums[i - 1];
                thisNumber = nums[i];
                nextNumber = nums[i + 1];
            } else if (thisNumber == nextNumber) {
                nextNumber = nums[i + 1];
            } else {
                preNumber = nums[i - 1];
                thisNumber = nums[i];
                nextNumber = nums[i + 1];
            }
        }
        if ((thisNumber > preNumber && thisNumber > nextNumber) || (thisNumber < preNumber && thisNumber < nextNumber)) {
            result++;
        }
        return result;
    }
}
```

# 2215.找出两数组的不同

```java
class Solution {
    public List<List<Integer>> findDifference(int[] nums1, int[] nums2) {
        Set<Integer> listSet1 = new HashSet<>();
        Set<Integer> listSet2 = new HashSet<>();
        List<Integer> list1 = new ArrayList<>();
        List<Integer> list2 = new ArrayList<>();
        Set<Integer> set1 = new HashSet<>();
        Set<Integer> set2 = new HashSet<>();
        for (int i = 0; i < nums1.length; i++) {
            set1.add(nums1[i]);
        }
        for (int i = 0; i < nums2.length; i++) {
            set2.add(nums2[i]);
            if (!set1.contains(nums2[i])) {
                listSet2.add(nums2[i]);
            }
        }
        for (int i = 0; i < nums1.length; i++) {
            if (!set2.contains(nums1[i])) {
                listSet1.add(nums1[i]);
            }
        }
        for (Integer key : listSet1) {
            list1.add(key);
        }
        for (Integer key : listSet2) {
            list2.add(key);
        }
        List<List<Integer>> result = new ArrayList<>();
        result.add(list1);
        result.add(list2);
        return result;
    }
}
```

# 2220.转换数字的最少位翻转次数

```java
class Solution {
    public int minBitFlips(int start, int goal) {
        List<Integer> startList = new ArrayList<>();
        List<Integer> goalList = new ArrayList<>();
        for (int i = 31; i >= 0; i--) {
            startList.add((start >> i) & 1);
            goalList.add((goal >> i) & 1);
        }
        int result = 0;
        for (int i = 0; i < startList.size(); i++) {
            if (startList.get(i) != goalList.get(i)) {
                result++;
            }
        }
        return result;
    }
}
```



# 2221.数组的三角和

```java
class Solution {
    public int triangularSum(int[] nums) {
        while (nums.length > 1) {
            int[] temp = new int[nums.length - 1];
            for (int i = 0; i < nums.length - 1; i++) {
                temp[i] = (nums[i] + nums[i + 1]) % 10;
            }
            nums = temp;
        }
        return nums[0];
    }
}
```



# 2224.转化时间需要的最小操作数

```java
class Solution {
    public int convertTime(String current, String correct) {
        String[] currentString = current.split(":");
        String[] correctString = correct.split(":");
        int result = (Integer.valueOf(correctString[0]) - Integer.valueOf(currentString[0])) * 60 + (Integer.valueOf(correctString[1]) - Integer.valueOf(currentString[1]));
        int cnt = 0;
        while (result > 0) {
            if (result >= 60) {
                result -= 60;
                cnt++;
            } else if (result >= 15) {
                result -= 15;
                cnt++;
            } else if (result >= 5) {
                result -= 5;
                cnt++;
            } else {
                result -= 1;
                cnt++;
            }
        } 
        return cnt;
    }
}
```

# 2225.找出输掉零场或一场比赛的玩家

```java
class Solution {
    public List<List<Integer>> findWinners(int[][] matches) {
        Set<Integer> playerSet = new HashSet<>();
        Map<Integer, Integer> loseMap = new HashMap<>();
        for (int i = 0; i < matches.length; i++) {
            playerSet.add(matches[i][0]);
            playerSet.add(matches[i][1]);
            loseMap.put(matches[i][1], loseMap.getOrDefault(matches[i][1], 0) + 1);
        }
        List<Integer> zeroList = new ArrayList<>();
        List<Integer> oneList = new ArrayList<>();
        for (Integer key : playerSet) {
            int cnt = loseMap.getOrDefault(key, 0);
            if (cnt == 0) {
                zeroList.add(key);
            }
            if (cnt == 1) {
                oneList.add(key);
            }
        }
        Collections.sort(zeroList);
        Collections.sort(oneList);
        List<List<Integer>> resultList = new ArrayList<>();
        resultList.add(zeroList);
        resultList.add(oneList);
        return resultList;
    }
}
```



# 2231.按奇偶性交换后的最大数字

```java
class Solution {
    public int largestInteger(int num) {
        String numString = String.valueOf(num);
        char[] numChar = numString.toCharArray();
        List<Integer> jiNumber = new ArrayList<>();
        List<Integer> jiIndex = new ArrayList<>();
        List<Integer> ouNumber = new ArrayList<>();
        List<Integer> ouIndex = new ArrayList<>();
        for (int i = 0; i < numChar.length; i++) {
            int tempInt = numChar[i] - '0';
            if (tempInt % 2 == 0) {
                ouNumber.add(tempInt);
                ouIndex.add(i);
            } else {
                jiNumber.add(tempInt);
                jiIndex.add(i);
            }
        }
        Collections.sort(jiNumber, Collections.reverseOrder());
        Collections.sort(ouNumber, Collections.reverseOrder());
        int[] result = new int[numChar.length];
        for (int i = 0; i < jiNumber.size(); i++) {
            int index = jiIndex.get(i);
            int number = jiNumber.get(i);
            result[index] = number;
        }
        for (int i = 0; i < ouNumber.size(); i++) {
            int index = ouIndex.get(i);
            int number = ouNumber.get(i);
            result[index] = number;
        }
        int resultNumber = 0;
        for (int i = 0; i < result.length; i++) {
            resultNumber = resultNumber * 10 + result[i];
        }
        return resultNumber;
    }
}
```

# 2232.向表达式添加括号后的最小结果

```java
class Solution {
    public String minimizeResult(String expression) {
        String[] numberStr = expression.split("\\+");
        String[] tempRes = new String[4];
        Integer min = 99999999;
        for (int i = 0; i < numberStr[0].length(); i++) {
            for (int j = 1; j <= numberStr[1].length(); j++) {
                String number1 = numberStr[0].substring(0, i);
                String number2 = numberStr[0].substring(i, numberStr[0].length());
                String number3 = numberStr[1].substring(0, j);
                String number4 = numberStr[1].substring(j, numberStr[1].length());
                if (number1.length() == 0) {
                    number1 = "1";
                }
                if (number4.length() == 0) {
                    number4 = "1";
                }
                Integer number1Int = Integer.valueOf(number1);
                Integer number2Int = Integer.valueOf(number2);
                Integer number3Int = Integer.valueOf(number3);
                Integer number4Int = Integer.valueOf(number4);
                Integer temp = (number2Int + number3Int) * number1Int * number4Int;
                if (temp < min) {
                    min = temp;
                    tempRes[0] = number2.equals(numberStr[0]) ? "" : number1;
                    tempRes[1] = number2;
                    tempRes[2] = number3;
                    tempRes[3] = number3.equals(numberStr[1]) ? "" : number4;
                }
            }
        }
        return tempRes[0] + "(" + tempRes[1] + "+" + tempRes[2] + ")" + tempRes[3];
    }
}
```



# 2235.两整数相加

```java
class Solution {
    public int sum(int num1, int num2) {
        return num1 + num2;
    }
}
```



# 2236.判断根结点是否等于子结点之和

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public boolean checkTree(TreeNode root) {
        return root.val == (root.left.val + root.right.val);
    }
}
```

# 2239.找到最接近0的数字

```java
class Solution {
    public int findClosestNumber(int[] nums) {
        int result = -999999;
        int len = 999999;
        for (int i = 0; i < nums.length; i++) {
            if (Math.abs(nums[i]) < len) {
                len = Math.abs(nums[i]);
                result = nums[i];
            } else if (Math.abs(nums[i]) == len) {
                result = Math.max(result, nums[i]);
            }
        }
        return result;
    }
}
```



# 2243.计算字符串的数字和

```java
class Solution {
    public String digitSum(String s, int k) {
        while (s.length() > k) {
            StringBuilder sb = new StringBuilder();
            for (int i = 0; i < s.length(); i+=k) {
                int temp = 0;
                for (int j = i; j < Math.min(i + k, s.length()); j++) {
                    temp += s.charAt(j) - '0';
                }
                sb.append(temp);
            }
            s = sb.toString();
        }
        return s;
    }
}
```



# 2248.多个数组求交集

```java
class Solution {
    public List<Integer> intersection(int[][] nums) {
        List<Integer> result = new ArrayList<>();
        int[] tempCnt = new int[1001];
        for (int i = 0; i < nums.length; i++) {
            for (int j = 0; j < nums[i].length; j++) {
                tempCnt[nums[i][j]]++;
            }
        }
        int cnt = nums.length;
        for (int i = 0; i < tempCnt.length; i++) {
            if (tempCnt[i] == cnt) {
                result.add(i);
            }
        }
        return result;
    }
}
```

# 2255.统计是给定字符串前缀的字符串数目

```java
class Solution {
    public int countPrefixes(String[] words, String s) {
        int result = 0;
        for (int i = 0; i < words.length; i++) {
            if (s.indexOf(words[i]) == 0) {
                result++;
            }
        }
        return result;
    }
}
```



# 2259.移除指定数字得到的最大结果

```java
class Solution {
    public String removeDigit(String number, char digit) {
        int result = 0;
        for (int i = 0; i < number.length(); i++) {
            if (number.charAt(i) == digit) {
                if (i < number.length() - 1 && number.charAt(i) < number.charAt(i + 1)) {
                    return number.substring(0, i) + number.substring(i + 1, number.length());
                }
                result = i;
            }
        }
        return number.substring(0, result) + number.substring(result + 1, number.length());
    }
}
```

# 2264.字符串中最大的3位相同数字

```java
class Solution {
    public String largestGoodInteger(String num) {
        String result = "-1";
        for (int i = 0; i < num.length() - 2; i++) {
            if (num.charAt(i) == num.charAt(i + 1) && num.charAt(i) == num.charAt(i + 2)) {
                String temp = num.substring(i, i + 3);
                if (Integer.valueOf(temp) > Integer.valueOf(result)) {
                    result = temp;
                }
            }
        }
        return "-1".equals(result) ? "" : result;
    }
}
```



# 2269.找到一个数字的K美丽值

```java
class Solution {
    public int divisorSubstrings(int num, int k) {
        char[] number = String.valueOf(num).toCharArray();
        int left = 0;
        int right = k - 1;
        int result = 0;
        while (right < number.length) {
            int temp = 0;
            for (int i = left; i <= right; i++) {
                temp = temp * 10 + (number[i] - '0');
            }
            if (temp != 0 && num % temp == 0) {
                result++;
            }
            left++;
            right++;
        }
        return result;
    }
}
```



# 2273.移除字母异位词后的结果数组

```java
class Solution {
    public List<String> removeAnagrams(String[] words) {
        List<char[]> tempList = new ArrayList<>();
        for (int i = 0; i < words.length; i++) {
            char[] tempChar = words[i].toCharArray();
            Arrays.sort(tempChar);
            tempList.add(tempChar);
        }
        List<String> resultList = new ArrayList<>();
        resultList.add(words[0]);
        char[] preList = tempList.get(0);
        for (int i = 1; i < tempList.size(); i++) {
            char[] temp1 = tempList.get(i);
            if (temp1.length != preList.length) {
                resultList.add(words[i]);
                preList = temp1;
                continue;
            }
            for (int j = 0; j < temp1.length; j++) {
                if (temp1[j] != preList[j]) {
                    resultList.add(words[i]);
                    preList = temp1;
                    break;
                }
            }
        }
        return resultList;
    }
}
```

# 2274.不含特殊楼层的最大连续楼层数

```java
class Solution {
    public int maxConsecutive(int bottom, int top, int[] special) {
        Arrays.sort(special);
        int start = special[0] - bottom;
        int end = top - special[special.length - 1];
        int max = Math.max(start, end);
        for (int i = 1; i < special.length; i++) {
            int temp = special[i] - special[i - 1] - 1;
            max = Math.max(temp, max);
        }
        return max;
    }
}
```



# 2278.字母在字符串中的百分比

```java
class Solution {
    public int percentageLetter(String s, char letter) {
        char[] ch = s.toCharArray();
        int count = 0;
        for (int i = 0; i < ch.length; i++) {
            if (letter == ch[i]) {
                count++;
            }
        }
        return count * 100 / ch.length;
    }
}
```

# 2283.判断一个数的数字计数是否等于数位的值

```java
class Solution {
    public boolean digitCount(String num) {
        int[] cnt = new int[11];
        for (int i = 0; i < num.length(); i++) {
            int temp = num.charAt(i) - '0';
            cnt[temp]++;
        }
        for (int i = 0; i < num.length(); i++) {
            int temp = num.charAt(i) - '0';
            if (cnt[i] != temp) {
                return false;
            }
        }
        return true;
    }
}
```

# 2284.最多单词数的发件人

```java
class Solution {
    public String largestWordCount(String[] messages, String[] senders) {
        Map<String, Integer> tempMap = new HashMap<>();
        for (int i = 0; i < messages.length; i++) {
            String[] tempWord = messages[i].split(" ");
            tempMap.put(senders[i], tempMap.getOrDefault(senders[i], 0) + tempWord.length);
        }
        String[][] tempRes = new String[tempMap.size()][2];
        int index = 0;
        for (String key : tempMap.keySet()) {
            tempRes[index][0] = key;
            tempRes[index][1] = String.valueOf(tempMap.get(key));
            index++;
        }
        Arrays.sort(tempRes, new Comparator<String[]>() {
            @Override
            public int compare(String[] o1, String[] o2) {
                if (o1[1].equals(o2[1])) {
                    return o2[0].compareTo(o1[0]);
                } else {
                    return Integer.compare(Integer.valueOf(o2[1]), Integer.valueOf(o1[1]));
                }
            }
        });
        return tempRes[0][0];
    }
}
```



# 2287.重排字符形成目标字符串

```java
class Solution {
    public int rearrangeCharacters(String s, String target) {
        int[] charList = new int[26];
        for (int i = 0; i < s.length(); i++) {
            char tempChar = s.charAt(i);
            charList[tempChar - 'a']++;
        }
        int result = 999;
        int[] targetCharList = new int[26];
        for (int i = 0; i < target.length(); i++) {
            char tempChar = target.charAt(i);
            targetCharList[tempChar - 'a']++;
        }
        for (int i = 0; i < charList.length; i++) {
            if (targetCharList[i] != 0) {
                result = Math.min(result, charList[i] / targetCharList[i]);
            }
        }
        return result;
    }
}
```

# 2293.极大极小游戏

```java
class Solution {
    public int minMaxGame(int[] nums) {
        while (nums.length > 1) {
            int[] tempList = new int[nums.length / 2];
            for (int i = 0; i < tempList.length; i++) {
                int temp1 = nums[i * 2];
                int temp2 = nums[i * 2 + 1];
                int result = -1;
                if (i % 2 == 0) {
                    result = Math.min(temp1, temp2);
                } else {
                    result = Math.max(temp1, temp2);
                }
                tempList[i] = result;
            }
            nums = tempList;
        }
        return nums[0];
    }
}
```

# 2295.替换数组中的元素

```java
class Solution {
    public int[] arrayChange(int[] nums, int[][] operations) {
        Map<Integer, Integer> tempMap = new HashMap<>();
        for (int i = 0; i < nums.length; i++) {
            tempMap.put(nums[i], i);
        }
        for (int i = 0; i < operations.length; i++) {
            int index = tempMap.get(operations[i][0]);
            tempMap.remove(operations[i][0]);
            tempMap.put(operations[i][1], index);
        }
        int[] result = new int[nums.length];
        for (Integer key : tempMap.keySet()) {
            result[tempMap.get(key)] = key;
        }
        return result;
    }
}
```



# 2299.强密码检验器Ⅱ

```java
class Solution {
    public boolean strongPasswordCheckerII(String password) {
        if (password.length() < 8) {
            return false;
        }
        boolean haveLowLetter = false;
        boolean haveUpLetter = false;
        boolean haveNumber = false;
        boolean haveSpecial = false;
        String specialString = "!@#$%^&*()-+";
        for (int i = 0; i < password.length(); i++) {
            char temp = password.charAt(i);
            if (i < password.length() - 1 && temp == password.charAt(i + 1)) {
                return false;
            }
            if (Character.isDigit(temp)) {
                haveNumber = true;
            }
            if (temp >= 'a' && temp <= 'z') {
                haveLowLetter = true;
            }
            if (temp >= 'A' && temp <= 'Z') {
                haveUpLetter = true;
            }
            if (specialString.indexOf(temp) != -1) {
                haveSpecial = true;
            }
        }
        return haveLowLetter && haveNumber && haveSpecial && haveUpLetter;
    }
}
```

