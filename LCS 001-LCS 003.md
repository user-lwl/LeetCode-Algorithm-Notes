# LCS 01.下载插件

```java
class Solution {
    public int leastMinutes(int n) {
        int size = 1;
        int minute = 0;
        while (size < n) {
            size *= 2;
            minute++;
        }
        return minute + 1;
    }
}
```

