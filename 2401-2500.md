# 2404.出现最频繁的偶数元素

```java
class Solution {
    public int mostFrequentEven(int[] nums) {
        Map<Integer, Integer> resultMap = new HashMap<>();
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] % 2 == 0) {
                resultMap.put(nums[i], resultMap.getOrDefault(nums[i], 0) + 1);
            }
        }
        int result = -1;
        int number = 999999;
        for (Integer key : resultMap.keySet()) {
            int cnt = resultMap.get(key);
            if (cnt > result) {
                result = cnt;
                number = key;
            } else if (cnt == result && key < number) {
                number = key;
            }
        }
        return result == -1 ? -1 : number;
    }
}
```



# 2405.子字符串的最优划分

```java
class Solution {
    public int partitionString(String s) {
        int[] temp = new int[26];
        char[] c = s.toCharArray();
        int max = 1;
        for (int i = 0; i < c.length; i++) {
            if (temp[(int)c[i] - 'a'] == 1) {
                temp = new int[26];
                max++;
            }
            temp[(int)c[i] - 'a']++;
        }
        return max;
    }
}
```

# 2409.统计共同度过的日子数

```java
class Solution {
    public int countDaysTogether(String arriveAlice, String leaveAlice, String arriveBob, String leaveBob) {
        int days = Math.min(dateToDays(leaveAlice), dateToDays(leaveBob)) - Math.max(dateToDays(arriveAlice), dateToDays(arriveBob)) + 1;
        return days < 0 ? 0 : days;
    }

    public int dateToDays(String date) {
        int month = Integer.valueOf(date.substring(0, 2));
        int day = Integer.valueOf(date.substring(3));
        int days = 0;
        for (int i = 0; i < month; i++) {
            if (i == 1 || i == 3 || i == 5 || i == 7 || i == 8 || i == 10 || i == 12) {
                days += 31;
            } else if (i == 2) {
                days += 28;
            } else {
                days += 30;
            }
        }
        days += day;
        return days;
    }
}
```



# 2410.运动员和训练师的最大匹配数

```java
class Solution {
    public int matchPlayersAndTrainers(int[] players, int[] trainers) {
        Integer[] playersInteger = Arrays.stream(players).boxed().toArray(Integer[]::new);
        Arrays.sort(playersInteger, new Comparator<Integer>() {
            @Override
            public int compare(Integer a, Integer b) {
                return b - a;
            }
        });
        Integer[] trainersInteger = Arrays.stream(trainers).boxed().toArray(Integer[]::new);
        Arrays.sort(trainersInteger, new Comparator<Integer>() {
            @Override
            public int compare(Integer a, Integer b) {
                return b - a;
            }
        });
        int person = 0;
        int train = 0;
        int max = 0;
        while (person < playersInteger.length && train < trainersInteger.length) {
            if (playersInteger[person] <= trainersInteger[train]) {
                max ++;
                person++;
                train++;
            } else {
                person++;
            }
        }
        return max;
    }
}
```

# 2413.最小偶倍数

```java
class Solution {
    public int smallestEvenMultiple(int n) {
        return n % 2 == 0 ? n : n * 2;
    }
}
```

# 2414.最长的字母序连续子字符串的长度

```java
class Solution {
    public int longestContinuousSubstring(String s) {
        int tempCnt = 1;
        int result = 1;
        s = s + "0";
        for (int i = 1; i < s.length(); i++) {
            if (s.charAt(i) - s.charAt(i - 1) == 1) {
                tempCnt++;
            } else {
                result = Math.max(tempCnt, result);
                tempCnt = 1;
            }
        }
        return result;
    }
}
```

# 2415.反转二叉树的奇数层

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public TreeNode reverseOddLevels(TreeNode root) {
        if (root == null) {
            return null;
        }
        doHelper(root.left, root.right, 1);
        return root;
    }
    public void doHelper(TreeNode left, TreeNode right, int level) {
        if (left == null || right == null) {
            return;
        }
        if (level % 2 == 1) {
            int temp = left.val;
            left.val = right.val;
            right.val = temp;
        }
        doHelper(left.left, right.right, level + 1);
        doHelper(left.right, right.left, level + 1);
    }
}
```



# 2418.按身高排序

```java
class Solution {
    public String[] sortPeople(String[] names, int[] heights) {
        int[][] heightsNum = new int[heights.length][2];
        for (int i = 0; i < heights.length; i++) {
            heightsNum[i][0] = heights[i];
            heightsNum[i][1] = i;
        }
        Arrays.sort(heightsNum, new Comparator<int[]>() {
            public int compare(int[] o1, int[] o2) {
                return o2[0] - o1[0];
            }
        });
        String[] result = new String[names.length];
        for (int i = 0; i < heightsNum.length; i++) {
            result[i] = names[heightsNum[i][1]];
        }
        return result;
    }
}
```

# 2423.删除字符使频率相同

```java
class Solution {
    public boolean equalFrequency(String word) {
        int[] number = new int[26];
        for (int i = 0; i < word.length(); i++) {
            number[word.charAt(i) - 'a']++;
        }
        Map<Integer, Integer> map = new HashMap<>();
        int keyTmp = -1;
        for (int i = 0; i < number.length; i++) {
            if (number[i] == 0) {
                continue;
            }
            keyTmp = number[i];
            map.put(number[i], map.getOrDefault(number[i], 0) + 1);
        }
        if (map.size() == 1 && (keyTmp == 1 || map.get(keyTmp) == 1)) {
            return true;
        }
        if (map.size() != 2) {
            return false;
        }
        int key1 = -1;
        int key2 = -1;
        int number1 = -1;
        int number2 = -1;
        for (Integer key : map.keySet()) {
            if (key1 == -1) {
                key1 = key;
                number1 = map.get(key);
            } else {
                key2 = key;
                number2 = map.get(key);
            }
        }
        System.out.println(key1 + " " + number1);
        System.out.println(key2 + " " + number2);
        if ((number1 == 1 && key1 - key2 == 1) || (number2 == 1 && key2 - key1 == 1) || (number1 == 1 && key1 == 1) || (number2 == 1 && key2 == 1)) {
            return true;
        }
        return false;
    }
}
```



# 2427.公因子的数目

```java
class Solution {
    public int commonFactors(int a, int b) {
        int result = 0;
        for (int i = 1; i <= Math.min(a, b); i++) {
            if (a % i == 0 && b % i == 0) {
                result++;
            }
        }
        return result;
    }
}
```

# 2428.沙漏的最大总和

```java
class Solution {
    public int maxSum(int[][] grid) {
        int max = 0;
        for (int i = 0; i < grid.length - 2; i++) {
            for (int j = 0; j < grid[i].length - 2; j++) {
                int temp = grid[i][j] + grid[i][j + 1] + grid[i][j + 2];
                temp += grid[i + 1][j + 1];
                temp += grid[i + 2][j] + grid[i + 2][j + 1] + grid[i + 2][j + 2];
                max = Math.max(max, temp);
            }
        }
        return max;
    }
}
```



# 2432.处理用时最长的那个任务的员工

```java
class Solution {
    public int hardestWorker(int n, int[][] logs) {
        int maxTime = -1;
        int id = -1;
        for (int i = 0; i < logs.length; i++) {
            int tempId = logs[i][0];
            int tempTime = i == 0 ? logs[i][1] : logs[i][1] - logs[i - 1][1];
            if (tempTime > maxTime) {
                maxTime = tempTime;
                id = tempId;
            } else if (tempTime == maxTime) {
                id = Math.min(id, tempId);
            }
        }
        return id;
    }
}
```

# 2433.找出前缀异或的原始数组

```java
class Solution {
    public int[] findArray(int[] pref) {
        int[] arr = new int[pref.length];
        arr[0] = pref[0];
        for (int i = 1; i < pref.length; i++) {
            arr[i] = pref[i - 1] ^ pref[i];
        }
        return arr;
    }
}
```



# 2437.有效时间的数目

```java
class Solution {
    public int countTime(String time) {
        String[] timeStamp = time.split(":");
        String hour = timeStamp[0];
        String second = timeStamp[1];
        int hourCnt = 1;
        int secondCnt = 1;
        if (hour.charAt(0) == '?' && hour.charAt(1) == '?') {
            hourCnt = 24;
        } else if (hour.charAt(0) == '?') {
            if (hour.charAt(1) <= '3') {
                hourCnt = 3;
            } else {
                hourCnt = 2;
            }
        } else if (hour.charAt(1) == '?') {
            if(hour.charAt(0) <= '1') {
                hourCnt = 10;
            } else {
                hourCnt = 4;
            }
        }

        if (second.charAt(0) =='?' && second.charAt(1) == '?') {
            secondCnt = 60;
        } else if (second.charAt(0) == '?') {
            secondCnt = 6;
        } else if (second.charAt(1) == '?') {
            secondCnt = 10;
        }

        return secondCnt * hourCnt;
    }
}
```



# 2441.与对应负数同时存在的最大正整数

```java
class Solution {
    public int findMaxK(int[] nums) {
        Set<Integer> tempSet = new HashSet<>();
        int result = -1;
        Arrays.sort(nums);
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] > 0) {
                if (tempSet.contains(nums[i])) {
                    result = Math.max(nums[i], result);
                }
            } else {
                tempSet.add(-nums[i]);
            }
        }
        return result;
    }
}
```

# 2442.反转之后不同整数的数目

```java
class Solution {
    public int countDistinctIntegers(int[] nums) {
        Set<Integer> tempSet = new HashSet<>();
        for (int i = 0; i < nums.length; i++) {
            StringBuilder sb = new StringBuilder();
            sb.append(nums[i]);
            sb.reverse();
            tempSet.add(nums[i]);
            tempSet.add(Integer.valueOf(sb.toString()));
        }
        return tempSet.size();
    }
}
```



# 2446.判断两个事件是否存在冲突

```java
class Solution {
    public boolean haveConflict(String[] event1, String[] event2) {
        int temp = Math.min(dateToMinute(event2[1]), dateToMinute(event1[1])) - Math.max(dateToMinute(event2[0]), dateToMinute(event1[0])) + 1;
        return temp > 0;
    }

    public int dateToMinute(String date) {
        int hour = Integer.valueOf(date.substring(0, 2));
        int minute = Integer.valueOf(date.substring(3));
        return hour * 60 + minute;
    }
}
```

# 2451.差值数组不同的字符串

```java
class Solution {
    public String oddString(String[] words) {
        Map<String, String> wordMap = new HashMap<>();
        Map<String, Integer> cntMap = new HashMap<>();
        for (int i = 0; i < words.length; i++) {
            StringBuilder sb = new StringBuilder();
            for (int j = 0; j < words[i].length() - 1; j++) {
                sb.append(words[i].charAt(j + 1) - words[i].charAt(j)).append(',');
            }
            wordMap.put(words[i], sb.toString());
            cntMap.put(sb.toString(), cntMap.getOrDefault(sb.toString(), 0) + 1);
        }
        String tempResult = "";
        for (String key : cntMap.keySet()) {
            if (cntMap.get(key) == 1) {
                tempResult = key;
            }
        }
        for (String key : wordMap.keySet()) {
            if (wordMap.get(key).equals(tempResult)) {
                return key;
            }
        }
        return "";
    }
}
```

# 2452.距离字典两次编辑以内的单词

```java
class Solution {
    public List<String> twoEditWords(String[] queries, String[] dictionary) {
        List<String> result = new ArrayList<>();
        next:
        for (int i = 0; i < queries.length; i++) {
            for (int j = 0; j < dictionary.length; j++) {
                int cnt = 0;
                for (int k = 0; k < queries[i].length(); k++) {
                    if (queries[i].charAt(k) != dictionary[j].charAt(k)) {
                        cnt++;
                    }
                    if (cnt > 2) {
                        break;
                    }
                }
                if (cnt <= 2) {
                    result.add(queries[i]);
                    continue next;
                }
            }
        }
        return result;
    }
}
```

# 2453.摧毁一系列目标

```java
class Solution {
    public int destroyTargets(int[] nums, int space) {
        Map<Integer, Integer> modMap = new HashMap<>();
        Map<Integer, Integer> minMap = new HashMap<>();
        for (int i = 0; i < nums.length; i++) {
            int mod = nums[i] % space;
            modMap.put(mod, modMap.getOrDefault(mod, 0) + 1);
            minMap.put(mod, Math.min(nums[i], minMap.getOrDefault(mod, (int) 1e9 + 1)));
        }
        int maxCntKey = -1;
        int maxCnt = -1;
        for (Integer key : modMap.keySet()) {
            int cnt = modMap.get(key);
            if (cnt > maxCnt) {
                maxCnt = cnt;
                maxCntKey = minMap.get(key);
            } else if (cnt == maxCnt) {
                maxCntKey = Math.min(maxCntKey, minMap.get(key));
            }
        }
        return maxCntKey;
    }
}
```



# 2455.可被三整除的偶数的平均值

```java
class Solution {
    public int averageValue(int[] nums) {
        int sum = 0;
        int cnt = 0;
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] % 3 == 0 && nums[i] % 2 == 0) {
                sum += nums[i];
                cnt++;
            }
        }
        return cnt == 0 ? 0 : sum / cnt;
    }
}
```

# 2457.美丽整数的最小增量

```java
class Solution {
    public long makeIntegerBeautiful(long n, int target) {
        long tempN = n;
        long tempRes = 0;
        while (tempN > 0) {
            tempRes += tempN % 10;
            tempN /= 10;
        }
        long result = 0;
        long tempDo = 10;
        while (tempRes > target) {
            long tempAdd = tempDo - n % tempDo;
            n += tempAdd;
            result += tempAdd;
            tempDo *= 10;
            tempRes = 0;
            tempN = n;
            while (tempN > 0) {
                tempRes += tempN % 10;
                tempN /= 10;
            }
        }
        return result;
    }
}
```



# 2460.对数组执行操作

```java
class Solution {
    public int[] applyOperations(int[] nums) {
        int[] result = new int[nums.length];
        int index = 0;
        for (int i = 0; i < nums.length; i++) {
            if (i == nums.length - 1) {
                if (nums[i] != 0) {
                    result[index] = nums[i];
                }
                break;
            }
            if (nums[i] == nums[i + 1]) {
                nums[i] *= 2;
                nums[i + 1] = 0;
            }
            if (nums[i] != 0) {
                result[index++] = nums[i];
            }
        }
        return result;
    }
}
```

# 2461.长度为K子数组中的最大和

```java
class Solution {
    public long maximumSubarraySum(int[] nums, int k) {
        Map<Integer, Integer> tempMap = new HashMap<>();
        long sum = 0;
        long max = 0;
        for (int i = 0; i < nums.length; i++) {
            sum += nums[i];
            tempMap.put(nums[i], tempMap.getOrDefault(nums[i], 0) + 1);
            if (i < k - 1) {
                continue;
            }
            if (tempMap.size() == k) {
                max = Math.max(max, sum);
            }
            sum -= nums[i - k + 1];
            int cnt = tempMap.get(nums[i - k + 1]);
            if (cnt == 1) {
                tempMap.remove(nums[i - k + 1]);
            } else {
                tempMap.put(nums[i - k + 1], cnt - 1);
            }
        }
        return max;
    }
}
```



# 2465.不同的平均值数目

```java
class Solution {
    public int distinctAverages(int[] nums) {
        Set<Double> tempSet = new HashSet<>();
        Arrays.sort(nums);
        int leftIndex = 0;
        int rightIndex = nums.length - 1;
        while (leftIndex < rightIndex) {
            Double avg = (double) (nums[leftIndex++] + nums[rightIndex--]) / 2;
            tempSet.add(avg);
        }
        return tempSet.size();
    }
}
```

# 2466.统计构造好字符串的方案数

```java
class Solution {
    public int countGoodStrings(int low, int high, int zero, int one) {
        int[] dp = new int[high + 1];
        dp[0] = 1;
        int result = 0;
        int mod = (int) 1e9 + 7;
        for (int i = 1; i < dp.length; i++) {
            if (i - zero >= 0) {
                dp[i] += dp[i - zero];
                dp[i] %= mod;
            }
            if (i - one >= 0) {
                dp[i] += dp[i - one];
                dp[i] %= mod;
            }
            if (i >= low && i <= high) {
                result += dp[i];
                result %= mod;
            }
        }
        return result;
    }
}
```



# 2469.温度转换

```java
class Solution {
    public double[] convertTemperature(double celsius) {
        return new double[]{celsius + 273.15, celsius * 1.8 + 32};
    }
}
```



# 2475.数组中不等三元组的数目

```java
class Solution {
    public int unequalTriplets(int[] nums) {
        Arrays.sort(nums);
        int result = 0;
        int len = nums.length;
        for (int i = 0, j = 0; i < len; i = j) {
            while (j < len && nums[j] == nums[i]) {
                j++;
            }
            result += i * (j - i) * (len - j);
        }
        return result;
    }
}
```

# 2481.分割圆的最少切割次数

```java
class Solution {
    public int numberOfCuts(int n) {
        if (n == 1) {
            return 0;
        }
        return n % 2 == 0 ? n / 2 : n;
    }
}
```

# 2482.行和列中一和零的差值

```java
class Solution {
    public int[][] onesMinusZeros(int[][] grid) {
        int[] onesRow = new int[grid.length];
        int[] zerosRow = new int[grid.length];
        int[] onesCol = new int[grid[0].length];
        int[] zerosCol = new int[grid[0].length];
        for (int i = 0; i < grid.length; i++) {
            for (int j = 0; j < grid[i].length; j++) {
                if (grid[i][j] == 1) {
                    onesRow[i]++;
                    onesCol[j]++;
                } else {
                    zerosRow[i]++;
                    zerosCol[j]++;
                }
            }
        }
        int[][] diff = new int[grid.length][grid[0].length];
        for (int i = 0; i < diff.length; i++) {
            for (int j = 0; j < diff[i].length; j++) {
                diff[i][j] = onesRow[i] + onesCol[j] - zerosRow[i] - zerosCol[j];
            }
        }
        return diff;
    }
}
```



# 2485.找出中枢整数

```java
class Solution {
    int[] prefix;
    public int pivotInteger(int n) {
        prefix = new int[n];
        prefix[0] = 1;
        for (int i = 1; i < n; i++) {
            prefix[i] = prefix[i - 1] + i + 1;
        }
        for (int i = 0; i < n; i++) {
            if (getPrefixSum(i) == getPrefixSum(n - 1) - getPrefixSum(i - 1)) {
                return i + 1;
            }
        }
        return -1;
    }
    public int getPrefixSum(int index) {
        if (index == -1) {
            return 0;
        }
        return prefix[index];
    }
}
```

# 2487.从链表中移除节点

```java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode removeNodes(ListNode head) {
        ListNode node = head;
        Stack<ListNode> tempStack = new Stack<>();
        while (node != null) {
            while (!tempStack.isEmpty() && tempStack.peek().val < node.val) {
                tempStack.pop();
            }
            tempStack.push(node);
            node = node.next;
        }
        ListNode tempNode = null;
        while (!tempStack.isEmpty()) {
            ListNode tmp = tempStack.pop();
            tmp.next = tempNode;
            tempNode = tmp;
        }
        return tempNode;
    }
}
```



# 2490.回环句

```java
class Solution {
    public boolean isCircularSentence(String sentence) {
        String[] word = (sentence + " " + sentence).split(" ");
        for (int i = 0; i < word.length / 2; i++) {
            if (word[i].charAt(word[i].length() - 1) != word[i + 1].charAt(0)) {
                return false;
            }
        }
        return true;
    }
}
```

# 2496.数组中字符串的最大值

```java
class Solution {
    public int maximumValue(String[] strs) {
        int result = 0;
        for (int i = 0; i < strs.length; i++) {
            boolean haveLetter = false;
            for (int j = 0; j < strs[i].length(); j++) {
                if (Character.isLetter(strs[i].charAt(j))) {
                    haveLetter = true;
                    break;
                }
            }
            if (haveLetter) {
                result = Math.max(result, strs[i].length());
            } else {
                result = Math.max(result, Integer.valueOf(strs[i]));
            }
        }
        return result;
    }
}
```



# 2500.删除每行中的最大值

```java
class Solution {
    public int deleteGreatestValue(int[][] grid) {
        int[] result = new int[grid[0].length];
        for (int i = 0; i < grid.length; i++) {
            Arrays.sort(grid[i]);
            for (int j = 0; j < grid[i].length; j++) {
                result[j] = Math.max(grid[i][j], result[j]);
            }
        }
        int sum = 0;
        for (int i = 0; i < result.length; i++) {
            sum += result[i];
        }
        return sum;
    }
}
```

